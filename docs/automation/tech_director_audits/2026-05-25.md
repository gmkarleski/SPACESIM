# Technical Director Daily Audit — SPACESIM

**Date:** 2026-05-25 (audit run at 18:15 UTC)
**Scope:** code activity, tests, architectural coherence, observation pass over the host repo as of HEAD = `adbcb96` (057a: migrate IsRoutineSupply from Vessel MonoBehaviour to VesselAuthoritativeState).
**Prior audit baseline:** `docs/automation/tech_director_audits/2026-05-24.md` (HEAD at that time was `fae79ac` / commit 056). One commit has landed in the intervening 24h.
**Sandbox note:** the sandbox working-tree view shows pending uncommitted changes (staged renames/deletes and multiple "modified" files) that do not appear in the canonical `git log` history. Per task-file guidance, host is canonical; findings below are sourced from `git log` and `git show HEAD:<path>` rather than working-tree diffs.

---

## Section 1: Code activity

One commit landed in the 24 hours preceding this run. This is a sharp drop from the 10-commit stack noted in the 2026-05-24 audit (which captured the 049–056 Phase 1 closing arc).

| Hash | Date (EDT) | Subject |
|---|---|---|
| `adbcb96` | 2026-05-24 16:34 | 057a: migrate IsRoutineSupply from Vessel MonoBehaviour to VesselAuthoritativeState |

Per-commit breakdown:

- **adbcb96 (057a)** — 5 files changed, +42 −54 lines net. Production C#: `VesselAuthoritativeState.cs` (+22), `IVessel.cs` (24 changed; net removal of `IsRoutineSupply` declaration plus migration-note comment retained), `Vessel.cs` (31 changed; net removal of field + property + migration-note comment retained), `VesselEventPredictionDriver.cs` (2 line single-call-site update at line 319). Test C#: `VesselEventPredictionDriverTests.cs` (17 changed; FakeVessel auto-property removed, four test sites updated). No asmdef change. No touch to `docs/CONSTRAINTS.md`, `docs/NETCODE_CONTRACT.md`, `docs/ARCHITECTURE.md`, or `docs/DECISIONS.md`. The commit message states a DECISIONS entry locking the supersession will land at a separate commit 057b, with a draft staged at repo root in `057_decisions_draft.md`.

No stacking pattern observed within the 24h window (single commit).

---

## Section 2: Test count and coverage state

Direct count via grep on `^\s*\[Test\]` / `^\s*\[UnityTest\]` over all `Tests/` and `PlayModeTests/` source files:

- EditMode total: **342** (Coordinates 63, SimTick 75, Vessels 204)
- PlayMode total: **6** (Coordinates 3, Vessels 3, SimTick 0)
- Tests added in last 24h: **0** (commit 057a is a field-migration; net `[Test]` change = 0)

No `[Ignore]` or `[Explicit]` attributes found anywhere in `SPACESIM/Assets/Scripts/Foundation/`. No `TestCaseSource` or `[Values]` parameterization in use. All tests are direct `[Test]` attributes.

**Discrepancy flag:** commit 057a's message claims "346 EditMode + 6 PlayMode = 352 green," but the actual `[Test]` attribute count is 342 + 6 = 348. The 2026-05-24 audit (HEAD = `fae79ac`) also reported 346 EditMode based on the commit message. The four-test gap is consistent across commits 056 and 057a, suggesting the figure was copy-forwarded in commit messages rather than re-counted. Surfacing as a stale-claim observation, not a coverage finding — the count itself is unchanged.

No tests with obviously skipped work (no `return;` early-out patterns inside `[Test]` methods in the recently-touched `VesselEventPredictionDriverTests.cs`). Single-`Assert.*` tests are present but spot-checks suggest they are intentional focused checks (e.g., `VesselTests.cs` has multiple narrowly-scoped tests by construction).

---

## Section 3: Architectural coherence

- **Three physics modes:** `PhysicsMode` enum in `SimTick/PhysicsMode.cs` declares `PhysXActive`, `KeplerRails`, `InterstellarCruise` exactly per contract. Mode dispatch in `SimTickController.cs:472–516` (Step6_DetectModeTransitions, Step7_PushAuthoritativeToPhysX) reads `PhysicsMode` via the `IActiveVessel` interface; no code observed bypassing the enum.
- **10-step cycle ownership:** `SimTickController.RunFixedUpdateCycle` at `SimTick/SimTickController.cs:325–361` owns the full Step1–Step10 dispatch. Step4_ApplyAnalyticUpdates is deliberately empty (`SimTickController.cs:438`), with analytic work running via TickAdvanced subscribers. No FixedUpdate logic was found outside this controller.
- **Driver pattern consistency:** `VesselTransitionDriver`, `VesselSoiRerootingDriver`, `VesselEventPredictionDriver` all share Initialize/Shutdown/ResetForTesting lifecycle, subscribe to `SimTickController.TickAdvanced`, and iterate `VesselRegistry.Vessels` with per-vessel try/catch. The XML doc on `VesselEventPredictionDriver.cs:23–28` calls out the deliberate parallel structure.
- **Asmdef boundaries:** confirmed via `using` directive scan — Coordinates references nothing else; SimTick references only Coordinates; Vessels references Coordinates and SimTick. No reverse references. SimTick contains no `using SpaceSim.Foundation.Vessels;` directives.
- **XML doc coverage:** spot-check via brace-aware grep flags only operator overloads (`==`/`!=` on `LocalPosition`, `WorldPosition`, `WarpHaltInfo`, `WarpRate`), one interface-implementation method (`FloatingOriginAnchor.OnFloatingOriginShifted` at `Coordinates/FloatingOriginAnchor.cs:88`), and one test-only seam (`ReferenceBody.InitializeBodyForTesting` at `Vessels/ReferenceBody.cs:283`) as undocumented. All concrete production types and methods of substance have XML docs.

No coherence concerns found.

---

## Section 4: Schema integrity

Field-by-field cross-check between `docs/NETCODE_CONTRACT.md` §2.1–§2.7 and the corresponding C# types:

- **VesselAuthoritativeState** (`Vessels/VesselAuthoritativeState.cs`): all 18 contract fields present, including `IsRoutineSupply` newly placed at lines 117–128 between `CrewAboard` and the mode-specific section, per contract ordering at NETCODE_CONTRACT §2.1 line 116.
- **PhysXState** (`Vessels/PhysXState.cs`): all 11 fields present.
- **KeplerState** (`Vessels/KeplerState.cs`): all 13 fields present, including the five `long?` event-prediction fields and `next_atmospheric_entry_tick` / `next_surface_impact_tick` per the commit 048 Stage 1 split.
- **CruiseState** (`Vessels/CruiseState.cs`): all fields present, with `TransmissionQueue` typed `List<object>` (stub) per contract Phase 0 scope language.
- **BodyState / ReferenceBody** (`Vessels/ReferenceBody.cs`): contract §2.7 fields `BodyId`, `MassKg`, `Mu`, `PositionWorld`, `SoiRadiusMeters`, `ParentBodyId`, `SurfaceRadiusMeters`, `AtmosphericTopAltitudeMeters` all present. Phase 4+ deferred fields documented in contract as not-yet-populated.

**One type-vs-doc mismatch flagged for the bot:** `PhysXState.cs:Orientation` is declared `public quaternion Orientation;` with XML doc claiming "double-precision quaternion." `Unity.Mathematics.quaternion` is a four-component f32 type — there is no double-precision quaternion in `Unity.Mathematics`. The contract (NETCODE_CONTRACT §2.2 line 152) writes `orientation: Quaternion // Orientation (quaternion, double-precision)`. Implementation uses f32 while contract specifies f64. This may be intentional (no double-precision quaternion available in the standard math library) or an oversight; either way, doc/contract and reality disagree.

Recent commit (057a) made a schema change that matches the contract: `IsRoutineSupply` was already in NETCODE_CONTRACT §2.1 at line 116 with the placement "between Crew assignment and Mode-specific sections," and the migration aligns implementation to that pre-existing contract specification.

---

## Section 5: Technical debt accumulation

Production-code scan (excludes Tests/ and PlayModeTests/):

- **TODO / FIXME / HACK comments:** zero found via `grep -rn "TODO\|FIXME\|HACK"` over `Foundation/`.
- **Phase X+ deferral markers:** 28 occurrences across 14 files. Highest concentration in `KeplerPropagator.cs`, `OrbitalElements.cs` (Phase 1 / Phase 4+ assumption boundaries documented in XML doc), `CruiseState.cs:12–14` (deferred to Phase 6 per artifact list), `SimEventType.cs:71, 77` (reserved enum values for Phase 5+ / Phase 6). All read as intentional design markers, not forgotten work.
- **Commented-out code blocks:** principal occurrence is `VesselDesign.cs:26–39`, a 14-line block of `//   public string DesignName;` style stubs explicitly labeled "PHASE 2 FIELDS (deferred)." Per its XML doc, this is intentional Phase 2 scaffolding.
- **Magic numbers in production:** `OrbitalElements.cs:775` (`1e-12`), `OrbitalElements.cs:907` (`1e-12`) — eccentricity-zero / denominator-zero tolerances inline. `ReferenceBody.cs:108` (`6.371e6` as default `surfaceRadiusMeters`) — has `Tooltip` annotation citing the source. The 053-stage2 commit extracted `PhysicsConstants.cs`; some constants still appear inline.
- **Methods over 100 lines:** `Vessel.cs:341` `TransitionToKeplerRails()` is 132 lines; `Vessel.cs:492` `TransitionToPhysXActive()` is 115 lines. Both are the mode-transition implementations; the length is structural to the symmetric protocol they implement.
- **Migration-note comments retained:** `Vessel.cs:76–79` (six lines) and `IVessel.cs:134–140` (seven lines) carry verbose comments explaining the 057a `IsRoutineSupply` migration. The 057a commit message explicitly says "Migration-note comments may be cleaned up in a future hygiene pass when the references become noise." **Candidate for mechanical fix** (deferred per author's own framing; flagged in Section 12).
- **Stale stub file at `Vessels/.../Coordinates/TestShiftDriver.cs`:** 16-line file containing only a comment block declaring "MOVED TO SimTick/ MODULE IN COMMIT 033" and instructing host-side `git rm`. The named destination `SPACESIM/Assets/Scripts/Foundation/SimTick/TestShiftDriver.cs` does not exist; functional replacement appears to live in `Vessels/TestVesselDriver.cs` per its header comment. Tracked in git. **Candidate for mechanical fix** — flagged in Section 12.
- **Stale `.probe` file:** `SPACESIM/Assets/Scripts/Foundation/Coordinates/FloatingOriginAnchor.cs.probe` (5 bytes, content "test") plus `.probe.meta`, both tracked in git since commit `589e20e` (commit 034). No production code references it; appears to be leftover diagnostic artifact. **Candidate for mechanical fix** — flagged in Section 12.

No god-class smell observed. `Vessel.cs` at 1150 lines is the largest production file; its responsibilities (mode-transition protocol, SOI re-rooting, trigger evaluation) are MonoBehaviour-owned and structurally cohesive.

---

## Section 6: Performance signals

- **Allocation in sim-tick loop:** `SimTickController.cs:87` (`EventPriorityQueue` member initializer) and `:112` (`_listeners` member initializer) are construction-time allocations, not per-tick. Spot read of Step1–Step10 method bodies shows no `new` allocations inside the cycle.
- **LINQ in production hot paths:** zero matches across `Foundation/` (`grep -rn "\.Where(\|\.Select(\|\.ToList(\|\.OrderBy("` excluding Tests/).
- **Reflection in production:** zero matches (`grep -rn "System.Reflection\|BindingFlags\|GetField(\|GetProperty(\|GetMethod("` excluding Tests/).
- **O(n²) iteration:** not observed in driver code; per-tick drivers iterate `VesselRegistry.Vessels` once (snapshot-iterate pattern).
- **String concatenation in tight loops:** the per-tick drivers (`VesselEventPredictionDriver.cs:217, 288, 331, 384, 420, 454`, `VesselSoiRerootingDriver.cs:176, 208`) use interpolated strings, but all sit inside exception-handler `Debug.LogError` paths or in rare-event log branches, not in normal-tick code. `SimTickController.cs:132–133` interpolation is the duplicate-singleton-warning path, not per-tick.

No performance-pattern concerns found for Phase 1.

---

## Section 7: Test infrastructure health

- **Reflection in tests:** nine test files import `System.Reflection`: `FloatingOriginManagerTests.cs`, `SimTickControllerTests.cs`, `AtmosphericEntryPredictorTests.cs`, `SoiCrossingPredictorTests.cs`, `SurfaceImpactPredictorTests.cs`, `VesselEventPredictionDriverTests.cs`, `VesselSoiRerootingDriverTests.cs`, `VesselTests.cs`, `VesselTransitionDriverTests.cs`. Most likely use is reading private state through test seams; the pattern is widespread enough that the tech-director-bot may want to track whether public test seams are growing in parallel.
- **LogAssert / ExpectLogMessage usage:** seven files including `FloatingOriginManagerPlayModeTests.cs`, `KeplerPropagatorTests.cs`, `VesselSoiRerootingDriverTests.cs`. Brittle-by-nature but used judiciously.
- **Test file sizes:** `VesselTests.cs` at **1590 lines** is approaching the 1500–2000 threshold called out in the SKILL.md. `VesselEventPredictionDriverTests.cs` at 1093, `OrbitalElementsTests.cs` at 1072 are the next largest. The 2026-05-24 audit's "vesseltests_split.md" artifact in `commits/` suggests prior thinking has gone into a split — observation, no candidate flagged.

---

## Section 8: Cross-asmdef and module dependency map

| Module | Files (count) | References (via using) | Asmdef references |
|---|---|---|---|
| Coordinates | 11 .cs (excl. tests, incl. 1 stale stub) | (none external to module) | Unity.Mathematics |
| SimTick | 13 .cs | Coordinates only | Coordinates, Unity.Mathematics, Unity.Burst, Unity.TextMeshPro |
| Vessels | 23 .cs | Coordinates, SimTick | Coordinates, SimTick, Unity.Mathematics, Unity.InputSystem |

Direction is one-way: Coordinates ← SimTick ← Vessels. No reverse or cyclic patterns. SimTick contains zero `using SpaceSim.Foundation.Vessels;` directives. Asmdef-listed references match observed using directives. No unexpected cross-asmdef calls.

---

## Section 9: Recurring code-level patterns

The `commits/` folder contains artifacts up through `051_multi_body_test_scene.md`; commits 052, 053, 054, 055, 056, 057a have no corresponding artifact files in `commits/`. Inspecting `commits/048_time_warp_controller.md`, `commits/049_active_vessel_camera_follow.md`, and `commits/051_multi_body_test_scene.md` for the Lessons sections:

- **048 Lesson "asmdef-direction-preserving pattern" (parallel-taxonomy enum, `WarpHaltReason` mirroring `TransitionTriggerReason`):** appears structurally landed (`SimTick/WarpHaltReason.cs` and the mapping function in `Vessels/VesselEventPredictionDriver.cs`). Has not recurred in subsequent code based on this audit's scan.
- **048 Lesson "reconnaissance catches what specs miss":** process lesson, not engineering-fix; no code-level recurrence to track.
- **049 Lesson "spec-vs-impl details" (`FloatingOriginManager.OriginShift` was actually `WorldToLocal`):** process lesson; the actual API name is in use at `FloatingOriginManager.cs` per the public surface.
- **051 Lesson "warp rates compress sim time":** no code-level fix; flagged as authoring-discipline for future scenes.

No engineering lesson from recent commits points at a tooling improvement (linter / analyzer / code review check) that hasn't happened. The Newton-Raphson convergence helper called out in earlier audits (`VesselTestHelpers.AssertSolvableEccentricity`, landed at commit 053-stage1) is present at `Vessels/Tests/VesselTestHelpers.cs` — eccentricity helper exists. The DECISIONS draft at `057_decisions_draft.md` explicitly closes this as resolved (entry D3).

---

## Section 10: Schema and architecture changes by file

Files touched in commit `adbcb96` (last 24h):

- **`VesselAuthoritativeState.cs`:** added `IsRoutineSupply` bool field at line 128, positioned between the `CrewAboard` section and the mode-specific section per NETCODE_CONTRACT §2.1 ordering. XML doc references DECISIONS commit 057 entry D1 and the supersession of commit 048 entry (e).
- **`IVessel.cs`:** removed `IsRoutineSupply` interface declaration; retained a seven-line migration-note comment at `:134–140` referring callers to `vessel.State.IsRoutineSupply`.
- **`Vessel.cs`:** removed `IsRoutineSupply` backing field and property; retained a six-line migration-note comment at `:76–79`.
- **`VesselEventPredictionDriver.cs`:** single per-tick reader at line 319 updated from `vessel.IsRoutineSupply` to `vessel.State.IsRoutineSupply`.
- **`VesselEventPredictionDriverTests.cs`:** `FakeVessel.IsRoutineSupply` auto-property removed; four test-site assignments at `:907`, `:965`, `:1004`, `:1025` updated to set the field via `_vessel.State.IsRoutineSupply = true;`.

No other Foundation files modified in the last 24h.

---

## Section 11: Raw observations

- **Repo-root scratch files present, not tracked in git:** `057_decisions_draft.md` (8.9 KB, mod 2026-05-24 20:01) plus eight `diff_*.txt` files at the repo root (`diff_051.txt`, `diff_constraints.txt`, `diff_decisions.txt`, `diff_phase_tracker.txt`, and five `diff_057a_*.txt` files dated 2026-05-24). Confirmed not in `git ls-files`. Likely working-context scratch from the 057a session; may not exist on the host repo since sandbox file presence can diverge from host per the SKILL.md guidance.
- **Commit artifacts gap:** `commits/` folder has artifacts numbered through `051_multi_body_test_scene.md`. Git history records `052`, `053-stage1`, `053-stage2`, `053-stage3`, `054`, `055`, `056`, `057a` — seven commits beyond the most-recent artifact. The 2026-05-24 audit captured this same gap at that time. Observation, not interpretation.
- **Sandbox working-tree divergence:** `git status` in the sandbox reports staged renames (`docs/stage4_setup_guide.md → docs/stage4_setup_guide`), staged deletions (`docs/vessels/STATUS.md`, `docs/world/STATUS.md`), and unstaged modifications to ten production and test files. These do not appear in `git log` HEAD content. Per SKILL.md, treated as sandbox quirk; not reported as findings.
- **Naming consistency:** `…Driver` suffix used by `VesselTransitionDriver`, `VesselSoiRerootingDriver`, `VesselEventPredictionDriver`, `TestVesselDriver`, `TestShiftDriver` (the stub). `…Manager` used for `FloatingOriginManager`. `…Controller` used for `SimTickController`, `WarpController`, `WarpUIController`, `ActiveVesselCameraFollow`. Pattern is roughly: Driver = per-tick subscriber, Manager = singleton lifecycle owner, Controller = orchestration. No obvious drift.
- **Quaternion type/doc mismatch (re-flag from Section 4):** `PhysXState.Orientation` field has `<summary>Vessel orientation (double-precision quaternion).</summary>` but is typed `Unity.Mathematics.quaternion` (f32). Either the comment should say "single-precision" or a double-precision quaternion implementation is intended. Flagging neutrally.

---

## Section 12: Mechanical fix candidates

Three candidates this cycle.

### Candidate 1: Delete stale `TestShiftDriver.cs` stub in Coordinates module

- **Source section:** Section 5
- **Scope summary:** The file `SPACESIM/Assets/Scripts/Foundation/Coordinates/TestShiftDriver.cs` is a 16-line stub containing only a comment block instructing host-side `git rm` (commit 033 framing). The named destination `SimTick/TestShiftDriver.cs` does not exist; functionality appears consolidated into `Vessels/TestVesselDriver.cs` per its header comment. The stub's `.meta` companion should be removed alongside.
- **Files affected:** `SPACESIM/Assets/Scripts/Foundation/Coordinates/TestShiftDriver.cs` and `SPACESIM/Assets/Scripts/Foundation/Coordinates/TestShiftDriver.cs.meta`.
- **Counts or quantities to verify:** Tech-director-bot should `git grep -l TestShiftDriver` on the host repo; the only matches should be these two stale files. Bot should confirm `SimTick/TestShiftDriver.cs` truly does not exist (audit confirms it isn't in `git ls-files`).
- **Why it's mechanical:** The stub explicitly self-documents as awaiting removal. No callers; no scene references should remain since commit 033 was 24 commits ago. Single-file deletion + meta.
- **Why it might not be mechanical:** If a Unity scene file somewhere (e.g., the test scenes) still references the `TestShiftDriver` class by the OLD namespace (`SpaceSim.Foundation.Coordinates`), the scene's component binding will go missing after deletion. Bot should verify no `.unity` file under `SPACESIM/Assets/Scenes/` contains a reference to the Coordinates-namespaced `TestShiftDriver` (e.g., search scenes for `m_Script:` GUIDs matching the stub's `.meta`). If a scene still binds it, the deletion needs scene cleanup, which is past mechanical scope.

### Candidate 2: Delete stale `FloatingOriginAnchor.cs.probe` diagnostic file

- **Source section:** Section 5
- **Scope summary:** `SPACESIM/Assets/Scripts/Foundation/Coordinates/FloatingOriginAnchor.cs.probe` is a 5-byte file containing the string `test`. Tracked in git since commit `589e20e` (commit 034 work). No production or test code references it. The `.probe.meta` companion exists alongside.
- **Files affected:** `SPACESIM/Assets/Scripts/Foundation/Coordinates/FloatingOriginAnchor.cs.probe` and `…cs.probe.meta`.
- **Counts or quantities to verify:** `git grep "FloatingOriginAnchor.cs.probe"` should produce no source-code hits; `git log -- "**/*.probe"` should show only commit 034 ingestion. `.gitignore` does not exclude `*.probe`.
- **Why it's mechanical:** Two-file deletion of a clearly diagnostic artifact with no inbound references and no described purpose in any current doc. Unity will not import `.probe` extensions (no scene linkage possible).
- **Why it might not be mechanical:** The `.probe` extension may be a workflow convention used elsewhere in the project (Cowork session probing pattern, for example). If so, deleting it could disturb a workflow not documented in code. Bot may want to confirm with Grayson whether `.probe` files have an intentional carry-forward role before deletion.

### Candidate 3: Remove migration-note comments in `Vessel.cs` and `IVessel.cs`

- **Source section:** Section 5 (and Section 10)
- **Scope summary:** Commit 057a's message explicitly states: "Migration-note comments may be cleaned up in a future hygiene pass when the references become noise." The retained comments are at `Vessel.cs:76–79` (six lines) and `IVessel.cs:134–140` (seven lines), both referring to the same 057a migration.
- **Files affected:** `SPACESIM/Assets/Scripts/Foundation/Vessels/Vessel.cs`, `SPACESIM/Assets/Scripts/Foundation/Vessels/IVessel.cs`.
- **Counts or quantities to verify:** Verify the migration-note comments are exactly 6 lines in `Vessel.cs` and 7 lines in `IVessel.cs` (audit observed these counts). Confirm no other call sites depend on the comment text for navigation (`grep -rn "057a"` across the repo).
- **Why it's mechanical:** Author of the commit explicitly flagged it as a future-pass-eligible cleanup. The comments carry no code semantics, only historical commentary. Two clean comment-block deletions.
- **Why it might not be mechanical:** Author wrote "when the references become noise" — discretionary timing. Doing it now may be premature; the comments aid readers encountering the field-on-State pattern who don't know the history. Also, the DECISIONS entry locking 057a is staged in `057_decisions_draft.md` and "lands at commit 057b"; removing the migration comments before 057b lands could fragment the historical trail. Bot may want to defer until 057b is committed.

No other mechanical candidates surfaced this cycle. The Section 11 quaternion type/doc mismatch is not flagged as mechanical because it could be a real type-conversion design choice (introducing a double-precision quaternion type vs. just fixing the comment); that's design, not mechanical.
