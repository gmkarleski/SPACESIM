# Technical Director Daily Audit — SPACESIM

**Date:** 2026-05-24 (audit run at 19:31 UTC)
**Scope:** code activity, tests, architectural coherence, and observation pass over `space_sim/` as of HEAD = `fae79ac` (056: Phase 1 closing hygiene).
**Prior audit baseline available:** `docs/automation/tech_director_audit_2026-05-21.md` (and one earlier file dated 2026-05-20). Day-over-day comparisons are made against the 2026-05-21 baseline where applicable. Where prior counts are not stated in that file, this run establishes a fresh baseline.
**Sandbox note:** the sandbox's working-tree view diverges from git HEAD (e.g., `PhysicsConstants.cs` truncates at 144 lines in the sandbox view, but `git show HEAD:` returns the canonical 165-line file). All findings below are sourced from `git show HEAD:<path>` and `git log` rather than direct working-tree reads; the host repo is canonical per the task contract.

---

## Section 1: Code activity

10 commits landed in the 24 hours preceding 2026-05-24 19:31 UTC. This is a heavy stacking pattern — closing-arc commits for Phase 1 (049–056) landed in a tight window. Listing newest-first:

| Hash | Date (EDT) | Subject |
|---|---|---|
| `fae79ac` | 2026-05-24 12:47 | 056: Phase 1 closing hygiene — scene revert + constants migration + helper migration + doc refresh |
| `bdbdbf5` | 2026-05-24 11:21 | 055: Phase 1 closure — doc reconciliation + push 049-055 block |
| `ccbc30b` | 2026-05-24 11:06 | 054: Phase 1 validation milestone — engine substrate validated |
| `3b4b040` | 2026-05-24 01:02 | 053-stage3: regression test for KeplerPropagator degenerate-orbit handling |
| `f114511` | 2026-05-24 00:52 | 053-stage2: PhysicsConstants extraction + degenerate-orbit guard + TransitionResult enum |
| `4091806` | 2026-05-24 00:33 | 053-stage1: eccentricity helper for Newton-Raphson convergence assertions |
| `c5de2d8` | 2026-05-24 00:15 | 052: Phase 1 validation incomplete — KeplerPropagator degenerate-orbit bug found |
| `16cc1d5` | 2026-05-23 23:06 | 051: multi-body TestVessels.unity with Moon + SOI-crossing test vessel |
| `e6a41fc` | 2026-05-23 21:52 | 050: commit Phase 1 validation readiness audit report |
| `3c25ab8` | 2026-05-23 21:29 | 049: active-vessel camera-follow for Play-mode visual validation |

Per-commit breakdown (P = production C#, T = test C#, D = docs/artifacts, S = scene, M = .meta):

- **fae79ac (056)** — 8 files, +68 −50. Production: `Coordinates/PhysicsConstants.cs` (+22), `SimTick/WarpController.cs` (+13). Tests: 6 files touched (AtmosphericEntryPredictorTests, OrbitalElementsTests, SoiCrossingPredictorTests, SurfaceImpactPredictorTests, VesselSoiRerootingDriverTests, VesselTestHelpers). No asmdef change. No CONSTRAINTS / NETCODE_CONTRACT / DECISIONS / ARCHITECTURE touch.
- **bdbdbf5 (055)** — 3 docs, +71 −20. CONSTRAINTS, DECISIONS, PHASE_TRACKER. **Architectural-doc touch:** CONSTRAINTS and DECISIONS.
- **ccbc30b (054)** — 1 new doc, +119: `docs/phase1_validation_results.md`. Pure validation artifact.
- **3b4b040 (053-stage3)** — 1 test file, +74. `VesselTests.cs` gains 1 EditMode regression test (`TransitionToKeplerRails_WhenPurelyRadialVelocity_LogsErrorAndReturnsFailedDegenerateOrbit`).
- **f114511 (053-stage2)** — 19 files, +369 −34. New production types: `Foundation/Coordinates/PhysicsConstants.cs`, `Foundation/Vessels/TransitionResult.cs`. Touched production: `ReferenceBody.cs`, `TestVesselDriver.cs`, `Vessel.cs`, `VesselTransitionDriver.cs`. 10 test files updated for `EarthMassKg`/`EarthMu` constant migration. No asmdef change. No CONSTRAINTS / NETCODE / DECISIONS / ARCHITECTURE touch.
- **4091806 (053-stage1)** — 3 files, +194. Test infra only: `VesselTestHelpers.cs` (+107 lines) + new `VesselTestHelpersTests.cs` (85 lines, 6 tests).
- **c5de2d8 (052)** — 1 new doc, +217. `docs/phase1_validation_incomplete.md`. Failure-mode artifact.
- **16cc1d5 (051)** — 4 files, +416 −22. Scene (`TestVessels.unity`), production (`TestVesselDriver.cs`), new artifact (`commits/051_multi_body_test_scene.md`), DECISIONS. **DECISIONS amendment.**
- **e6a41fc (050)** — 1 new doc, +407. `docs/phase1_validation_readiness.md`.
- **3c25ab8 (049)** — 5 files, +251. New production file `SimTick/ActiveVesselCameraFollow.cs` (171 lines + .meta), scene update, new artifact, DECISIONS amendment. **DECISIONS amendment.**

**Stacked-commit pattern:** 7 commits landed in a single 4.5-hour window between 21:29 EDT (5/23) and 01:02 EDT (5/24). A second cluster of 3 commits landed in a 1-hour window at 11:06–12:47 EDT (5/24). This is the Phase 1 closing arc pattern — bug-find at 052, three-stage fix (053-stage1/2/3), validation milestone (054), doc reconciliation (055), final hygiene (056).

**Architectural-doc activity:** DECISIONS amended 3× in the 24-hour window (049, 051, 055). CONSTRAINTS amended 1× (055). NETCODE_CONTRACT untouched since commit 048-stage1 (`f9200ac`, prior to the window). ARCHITECTURE untouched in the window. No asmdef boundary changes.

---

## Section 2: Test count and coverage state

Counts sourced from `git show HEAD:<file> | grep -cF "[Test]"` etc., with doc-comment occurrences excluded by inspecting line locations.

**Total: 346 EditMode + 6 PlayMode = 352 green** (matches commit 056's stated count).

EditMode breakdown:

| Module | Tests |
|---|---|
| Coordinates | 63 (CoordinateMath 14, FloatingOriginManager 28, LocalPosition 8, WorldPosition 13) |
| SimTick | 75 (EventPriorityQueue 12, SimTickController 31, WarpController 32) |
| Vessels | 208 (Atmospheric 6, BodyRegistry 10, KeplerPropagator 15, OrbitalElements 38, PeriapsisApoapsis 11, SoiCrossing 14, SurfaceImpact 5, VesselEventPredictionDriver 26, VesselRegistry 8, VesselSoiRerootingDriver 4, VesselTestHelpers 6, VesselTests 59, VesselTransitionDriver 6) |
| **EditMode total** | **346** |

PlayMode breakdown: 3 in `Coordinates/PlayModeTests/FloatingOriginManagerPlayModeTests.cs` (lines 54, 63, 104) + 3 in `Vessels/PlayModeTests/VesselPlayModeTests.cs` (lines 81, 108, 133) = **6**.

**Tests added in last 24 hours: 7 (all behavior tests, no stubs).**

| Commit | File | Count | Classification |
|---|---|---|---|
| 4091806 (053-stage1) | VesselTestHelpersTests.cs (new) | 6 | 4 boundary/defensive (NaN, Infinity, threshold) + 2 behavior (well-formed input passes / threshold-exceeded throws). All assert via `Assert.Throws<AssertionException>` or `Assert.DoesNotThrow`. |
| 3b4b040 (053-stage3) | VesselTests.cs | 1 | Behavior test — exact-conditions regression for commit 052's purely-radial-velocity NaN bug. Uses `LogAssert.Expect` for guard error + `Assert.AreEqual` for enum result + `Assert.IsNull` for KeplerState absence + `Assert.AreEqual` for mode preservation. 4 assertions + 1 LogAssert + 1 `Assert.Throws` sanity guard — well-covered. |

**Stub-to-behavior ratio:** no stub tests added in window. All 7 additions are real behavior coverage with multiple assertions.

**Flag scan results:**

- `[Ignore]` / `[Explicit]` attributes in `SPACESIM/Assets/Scripts/Foundation/`: **none found.**
- Tests using `LogAssert.Expect` (legitimate but brittle pattern): present in the new degenerate-orbit regression test (`VesselTests.cs:894`-ish region). Uses a regex matcher against the stable static portion of the guard message, deliberately avoiding coupling to runtime float interpolations — documented in the test body. Acceptable pattern.
- Very-small-assertion tests: the 6 new helper-validation tests in VesselTestHelpersTests are by nature single-assertion (each `Assert.Throws` or `Assert.DoesNotThrow` is a one-liner). Appropriate for the helper-validation use case; not under-tested.

---

## Section 3: Architectural coherence

LOCKED items from CONSTRAINTS / NETCODE_CONTRACT verified against current HEAD code:

**Three physics modes correctly enum'd.** `SimTick/PhysicsMode.cs` declares `PhysXActive`, `KeplerRails`, `InterstellarCruise`. Mode dispatch lives in `Vessel.cs` (`Mode` property reads `_state.Mode`) and the transition drivers (`VesselTransitionDriver`, `VesselSoiRerootingDriver`, `VesselEventPredictionDriver`). The mode set matches CONSTRAINTS §2 "Physics architecture" exactly. No code path bypasses the mode dispatch surface observed in the production code; `WarpController.CeilingFor` dispatches on `PhysicsMode` correctly.

**Sim-tick controller owns the 10-step cycle.** `SimTickController.RunFixedUpdateCycle()` (around line 320–365) sets `CurrentPhase` to each `SimTickPhase` enum value sequentially: `ReceivePeerState → ReadPhysX → ConvertToAuthoritative → ApplyAnalyticUpdates → ReconcileAuthoritative → DetectModeTransitions → PushAuthoritativeToPhysX → ReplicateToPeers → FireEvents → AdvanceCounter → Idle`. Matches the NETCODE_CONTRACT §1.2 ten-step ordering exactly. No `FixedUpdate` logic observed outside this controller in the Foundation source.

**Vessel container schema vs NETCODE_CONTRACT §2.1.** Field-by-field comparison of `VesselAuthoritativeState.cs` against the contract (see Section 4 for the full schema-drift analysis). **One contract field is missing from `VesselAuthoritativeState` and lives on the runtime `Vessel` MonoBehaviour instead:** `is_routine_supply` (contract) ↔ `Vessel.IsRoutineSupply` / `Vessel.isRoutineSupply` (`Vessel.cs:98, 102`). This is flagged in Section 4 as a possible-intentional split that the tech-director-bot should adjudicate; not a clear mistake.

**Driver pattern consistency.** All three driver classes follow the contract:
- `VesselTransitionDriver` subscribes to `SimTickController.Instance.TickAdvanced` in `OnEnable`/`OnDisable`, iterates `VesselRegistry`, performs per-vessel transition evaluation.
- `VesselSoiRerootingDriver` same pattern.
- `VesselEventPredictionDriver` same pattern + writes into `EventQueue`.

No newly-added "should be a driver but isn't" candidate observed in the 24-hour window. `ActiveVesselCameraFollow` (new, commit 049) is a `MonoBehaviour` reading `SimTickController.Instance.ActiveVessel` from `LateUpdate`, which is a presentation-layer reader (not a driver), and the file documents the choice explicitly.

**Asmdef boundaries.** Checked via `using` directives in each module's production source (XML-doc `<see cref="...">` references don't induce real dependencies):
- `Coordinates` → only `Unity.Mathematics` (no SimTick / Vessels references). Clean.
- `SimTick` → `SpaceSim.Foundation.Coordinates`, `Unity.Mathematics`, `Unity.Burst`, `Unity.TextMeshPro` (latter via UI/WarpUIController). No Vessels dependency. Clean.
- `Vessels` → `SpaceSim.Foundation.Coordinates`, `SpaceSim.Foundation.SimTick`, `Unity.Mathematics`, `Unity.InputSystem`. Clean.

Dependency direction is acyclic and one-directional: `Coordinates ← SimTick ← Vessels`. No reverse references observed.

**XML doc coverage.** Spot-check across new and modified production files (`PhysicsConstants.cs`, `TransitionResult.cs`, `ActiveVesselCameraFollow.cs`, `WarpController.cs`, `Vessel.cs`): all public types, properties, and methods carry `<summary>` blocks. Doc density is high — typical type has 30+ lines of preamble before the first member.

---

## Section 4: Schema integrity

NETCODE_CONTRACT §2 vs C# implementations. Cross-checked field-by-field.

**§2.1 VesselAuthoritativeState (top-level fields):**

| Contract field | Type | C# implementation | Match? |
|---|---|---|---|
| `vessel_id` | UUID | `VesselAuthoritativeState.VesselId : Guid` | ✓ |
| `design_id` | UUID | `DesignId : Guid` | ✓ |
| `name` | String | `Name : string` | ✓ |
| `owner_agency_id` | UUID | `OwnerAgencyId : Guid` (stub default `Guid.Empty`) | ✓ |
| `authority_holder_id` | NetworkID | `AuthorityHolderId : string` (placeholder type) | ✓ (typed-stub acknowledged in XML doc) |
| `mode` | PhysicsMode | `Mode : PhysicsMode` | ✓ |
| `mode_entered_at` | SimTickCount | `ModeEnteredAtTick : long` | ✓ |
| `total_mass_kg` | f64 | `TotalMassKg : double` | ✓ |
| `center_of_mass_local` | f64x3 | `CenterOfMassLocal : double3` | ✓ |
| `moments_of_inertia` | f64x3 | `MomentsOfInertia : double3` | ✓ |
| `resource_inventory` | ResourceMap | `ResourceInventory : Dictionary<string,double>` (stub) | ✓ (stub-typed) |
| `crew_aboard` | List<CrewID> | `CrewAboard : List<Guid>` | ✓ |
| `is_routine_supply` | bool | **NOT ON `VesselAuthoritativeState`** — implemented as `Vessel.IsRoutineSupply` (`Vessel.cs:98`) backed by `[SerializeField] private bool isRoutineSupply` (`Vessel.cs:102`). | **MISMATCH — see analysis below.** |
| `physx_state` | Option<PhysXState> | `PhysXState : PhysXState` (nullable reference) | ✓ |
| `kepler_state` | Option<KeplerState> | `KeplerState : KeplerState` | ✓ |
| `cruise_state` | Option<CruiseState> | `CruiseState : CruiseState` | ✓ |
| `telemetry_buffer` | CircularBuffer | `TelemetryBuffer : List<object>` (stub) | ✓ (stub-typed) |
| `last_advanced_tick` | SimTickCount | `LastAdvancedTick : long` | ✓ |

**Schema-drift finding (`is_routine_supply`):** the NETCODE_CONTRACT places `is_routine_supply` on the authoritative state (the save-file shape, per the contract's framing in §2). The implementation places it on the `Vessel` MonoBehaviour instead. If this is intentional (a deliberate runtime-vs-authoritative-state separation for the supply-classification flag), the contract is out of date and should be amended. If this is unintentional, the field needs to move onto `VesselAuthoritativeState` before any save/load implementation lands — otherwise saves won't round-trip the routine-supply classification. Cannot determine intent from the source comments alone — `Vessel.cs:102` doesn't explain why the field lives there and not on the state struct. **Flagging for tech-director-bot adjudication.**

**§2.2 PhysXState:** all 11 contract fields present in `PhysXState.cs` with matching types (`position_world ↔ PositionWorld`, `velocity_world ↔ VelocityWorld`, `orientation ↔ Orientation`, `angular_velocity ↔ AngularVelocity`, `reference_body_id ↔ ReferenceBodyId`, `floating_origin ↔ FloatingOrigin`, `rigidbody_handle ↔ RigidbodyHandle`, `active_thrust_n ↔ ActiveThrustN`, `active_thrust_direction ↔ ActiveThrustDirection`, `atmospheric_density ↔ AtmosphericDensity`, `atmospheric_velocity_rel ↔ AtmosphericVelocityRel`). No drift.

**§2.3 KeplerState:** all contract fields present with correct nullable typing on the four predictor fields (`NextPeriapsisTick`, `NextApoapsisTick`, `NextSoiTransitionTick`, `NextAtmosphericEntryTick`, `NextSurfaceImpactTick`). The commit-048-stage-1 split of `NextModeTransitionTick` into `NextAtmosphericEntryTick` + `NextSurfaceImpactTick` is reflected both in contract (commit 048 amendment) and code. No drift.

**§2.4 CruiseState:** all contract fields present in `CruiseState.cs` as Phase 6 stubs (typed-shell). No drift.

**§2.7 BodyState:** Phase 0/1 in-code fields (`MassKg`, `Mu`, `PositionWorld`, `SoiRadiusMeters`, `ParentBodyId`, `SurfaceRadiusMeters`, `AtmosphericTopAltitudeMeters`) all present in `ReferenceBody.cs`. Phase 4+ deferred fields (orbital state, axial tilt, rotation rate, terrain seed, atmospheric profile, children list) correctly absent. No drift.

**§2.6 WorldAuthoritativeState `current_warp_rate` (commit 048-stage-1):** contract specifies `(i64, i64)` rational. `WarpController.WarpRate` struct uses `long Numerator` + `long Denominator`. No drift.

**Recent commits adding fields:** the only field-level schema work in the window was 053-stage2's introduction of `TransitionResult` enum — that's a return-type enum used by `Vessel.TransitionToKeplerRails`, not part of the authoritative state schema. No NETCODE_CONTRACT amendment was required; correctly omitted. The 053-stage2 commit also added `PhysicsConstants.DegenerateOrbitAngularMomentumSquaredScale` — a solver-stability tunable, not a contract field. Correctly omitted.

---

## Section 5: Technical debt accumulation

**TODO / FIXME / HACK / XXX comments in Foundation production code:** zero hits via `grep -rn -E "TODO|FIXME|HACK|XXX"` against `SPACESIM/Assets/Scripts/Foundation/`. The codebase is clean on these markers.

**"Phase X+" / "Stage Y will" deferral markers (production code only):**

| File:line | Marker context |
|---|---|
| `SimTick/WarpController.cs:65` | "Stage 4 adds the scene wiring" — **stale forward-reference; Stage 4 already landed at commit 8ce6e0f (048-stage4, 2026-05-23). Commit 056 explicitly updated two other stale Stage-3 forward-references in the same file at lines ~24 and ~340 but appears to have missed this one.** [candidate for mechanical fix] |
| `SimTick/EventPriorityQueue.cs:29` | "Phase 1 (1-2 vessels × ≤7 event types). Performance is not the primary…" — Phase-1-scope statement; not a deferral. |
| `Coordinates/CoordinateMath.cs:25-27` | "Lives here in Phase 0 because there's exactly one physical constant…" — superseded by commit 053-stage2's `PhysicsConstants.cs` extraction; comment may be stale. [candidate for mechanical fix, see Section 12] |
| `Vessels/CruiseState.cs:13-14` | "Phase 6 implementation deferred" — accurate documentation of the stub. |
| `Vessels/KeplerPropagator.cs:35` | Parabolic orbit (e == 1.0) deferral — accurate documentation. |
| `Vessels/BodyRegistry.cs:100` | "O(N) iteration… Phase 1 / early-Phase-4 scale" — performance scope note. |
| `Vessels/OrbitalElements.cs:315, 326, 334` | Phase 1 limitation comments around `ReRootStateVector` — accurate. |
| `Vessels/AtmosphericEntryPredictor.cs:22, 40, 80, 93` | Phase 0/1 vs Phase 4+ scope notes — accurate. |
| `SimTick/ActiveVesselCameraFollow.cs:18, 24, 65` | "Phase 1 foundations end-to-end visually" / "Phase 3 alongside…" — accurate scope notes for a Phase 1 utility. |
| `SimTick/SimEventType.cs:71, 77` | Reserved enum values for Phase 5+/Phase 6 — accurate. |
| `SimTick/UI/WarpUIController.cs:42, 43, 49, 53` | Phase 4 debug UI scope notes — accurate. |
| `PrototypeStartupTest.cs:6, 11, 23` | Phase 0 smoke test — accurate description of a debug-output file. |

**Commented-out code blocks:** none observed in spot-checks of production code.

**Magic number literals:** noted by the project's own audit-driven migrations. Remaining call sites that fit the constants-extraction pattern:

- `Vessels/Tests/SoiCrossingPredictorTests.cs:34` declares `private const double MoonSoiRadiusMeters = 6.6e7;` (precision mismatch vs `PhysicsConstants.MoonSoiRadiusMeters = 6.6183e7`). Commit 056 explicitly preserved this site "pending separate fixer-bot pass." Acknowledged, scheduled. [candidate for mechanical fix — but cross-precision sensitivity must be verified per call site]
- `Vessels/Tests/OrbitalElementsTests.cs:319` declares the same `private const double MoonSoiRadiusMeters = 6.6e7;`. Same precision-mismatch story. [candidate for mechanical fix, with same caveat]
- `Vessels/Vessel.cs:800, 809` — `AtmosphericDensityThreshold = 1e-6` and `ProximityThresholdMeters = 50_000.0` are file-private consts. Both have semantic documentation; not duplicated elsewhere. Not a fix candidate now.

**Methods over 100 lines:** spot-check on the largest production files (`Vessel.cs` at 1145 lines, `OrbitalElements.cs` at 978 lines, `SimTickController.cs` at 575 lines, `SoiCrossingPredictor.cs` at 460 lines, `VesselEventPredictionDriver.cs` at 458 lines, `WarpController.cs` at 448 lines, `ReferenceBody.cs` at 346 lines) — no concrete length measurement done per-method this run; the line counts are at the upper end for files of their responsibility but each is a single coherent type. Worth a per-method audit in a future cycle if growth continues.

**God-class smell:** `Vessel.cs` is the largest production file at 1145 lines. It owns mode transitions, the rigidbody, the authoritative state, the routine-supply flag, and exposure of IVessel. The growth pattern is integrative-by-design (a vessel is the touch-point for many systems), but the file is approaching the size where the next added responsibility deserves a "should this be a satellite class?" question. Not a fix candidate; observation for the bot.

**Duplicated code patterns:** `OrbitalElements.SolveConicAtRadius` was extracted at commit 047 to absorb the duplicated math from atmospheric + surface + SOI-outward predictors — that pattern is now resolved. The `BoundaryHugTolerance` constant referenced inline at `OrbitalElements.cs:759, 760` is read from the public const at line 493, not duplicated. No new duplication patterns spotted in the 24-hour window.

---

## Section 6: Performance signals

For Phase 1, performance is not the primary concern; this section is for forward-looking architectural-perf observations.

**Allocations inside the sim-tick loop (`SimTickController.RunFixedUpdateCycle` and its called steps):** quick scan of `SimTickController.cs` shows the only listener-list-related state is `private readonly List<ISimTickListener> _listeners = new List<ISimTickListener>()` at line 112, allocated once at field-initialization. Iteration uses `foreach` over the field — no per-tick allocations observed in the controller spine. `EventQueue` operations (`PeekTopTick`, push/pop) are present; whether they allocate depends on the underlying queue implementation (see `EventPriorityQueue.cs`). Did not deep-trace queue internals this cycle; flag for a future targeted perf pass.

**LINQ in hot paths:** zero matches for `\.Where|\.Select|\.ToList|\.OrderBy` across `SPACESIM/Assets/Scripts/Foundation/` production code. Clean.

**O(n²) iteration with unbounded n:** none observed in the 24-hour window. `VesselEventPredictionDriver` iterates the vessel registry once per tick and produces per-vessel predictions (O(n)), with each prediction being a constant-time-ish analytic solve.

**Reflection in production code:** none observed (only typeof() in `TestVesselDriver` for `[RequireComponent(typeof(Vessel))]`, which is the Unity attribute pattern and not runtime reflection).

**String concatenation in tight loops:** none observed. String building appears only in error-log paths (`Vessel.cs:453` interpolated string inside the degenerate-orbit error log), which are fire-once-on-bad-input paths, not hot loops.

---

## Section 7: Test infrastructure health

Last 5 commits' worth of test changes: 053-stage1 (helper additions + new test file), 053-stage2 (test-side `EarthMassKg`/`EarthMu` constant migrations across 10 files), 053-stage3 (1 regression test added), 056 (11 `Assert.Less(e, 0.8, ...)` migrations → `VesselTestHelpers.AssertSolvableEccentricity`, plus `EarthMoonDistanceMeters` removal from `VesselTestHelpers`). All within the 24-hour audit window.

**Tests reading private fields via reflection:** the test-seam migration thread in commits 620e5b1 ("14 of 15 ReferenceBody reflection sites migrated to parameterized `InitializeBodyForTesting` overload") and 3a472a1 ("VesselTests.cs split at driver-test seam") landed prior to the audit window but explains why reflection patterns are now minimal. No new reflection-based-mutation tests added in the 24-hour window. The 053-stage3 regression test uses public APIs (`Initialize`, `Rigidbody.position/linearVelocity`, `TransitionToKeplerRails`) — clean.

**Setup-helper usage:** `VesselTestHelpers` grew at 053-stage1 with `AssertSolvableEccentricity` + `AssertNonDegenerateOrbit`. The pattern is a static helper class with `using static SpaceSim.Foundation.Vessels.Tests.VesselTestHelpers;` at consumer sites — usage stays explicit-at-call-site without `[SetUp]` fixture growth. Helpers themselves now have a test file (`VesselTestHelpersTests.cs`, 053-stage1), which is good infra hygiene.

**Tests asserting on log messages:** the new regression test in `VesselTests.cs` uses `LogAssert.Expect(LogType.Error, new Regex(@"…stable static portion…"))` — explicitly chooses a regex to decouple from runtime float interpolations. Documented in the test body. Acceptable.

**PlayMode vs EditMode appropriateness:** the 7 new tests in the window are all EditMode (pure C# math + `[Test]` attribute). Appropriate — none of them need the play loop.

**File-size growth:** test files in the Vessels/Tests/ folder:

| File | Lines |
|---|---|
| VesselTests.cs | 1590 (sandbox)/+74 lines this commit — true line count from `git show` should be checked |
| VesselEventPredictionDriverTests.cs | 1094 |
| OrbitalElementsTests.cs | 1075 |
| SoiCrossingPredictorTests.cs | 681 |
| KeplerPropagatorTests.cs | 530 |
| SimTickControllerTests.cs | 531 |
| WarpControllerTests.cs | 494 |
| FloatingOriginManagerTests.cs | 422 |
| PeriapsisApoapsisPredictorTests.cs | 357 |

`VesselTests.cs` was split at 3a472a1 (`operational: VesselTests.cs split at driver-test seam — 25 tests migrated… file reduced 2625-1620 lines`), and 053-stage3 added 74 more lines for the regression test. The file is back on a growth curve; once it crosses ~2000 lines another seam-split pass should be considered. `VesselEventPredictionDriverTests.cs` at 1094 lines and `OrbitalElementsTests.cs` at 1075 lines are both on the watch-list but below the 1500-line threshold.

**Stale `using` directives / dead helpers:** commit 620e5b1 (`operational: 14 of 15 ReferenceBody reflection sites migrated… SoiCrossing Set* helpers and Atmospheric SetField helper removed as dead code`) handled the bulk of the dead-helper cleanup prior to the window. The 056 commit's `EarthMoonDistanceMeters / MoonMassKg / MoonSoiRadiusMeters` removal from `VesselTestHelpers.cs` is the latest pass. Spot-checked `VesselTestHelpers.cs` head — no dead `using` directives observed after the migration. Did not run a comprehensive stale-using sweep this cycle.

---

## Section 8: Cross-asmdef and module dependency map

**Coordinates module** (`SpaceSim.Foundation.Coordinates`):
- Files: CoordinateMath.cs, FloatingOriginAnchor.cs, FloatingOriginManager.cs, IFloatingOriginListener.cs, LocalPosition.cs, PhysicsConstants.cs, TestShiftDriver.cs, WorldPosition.cs
- References (asmdef): Unity.Mathematics
- Production `using` directives: only Unity-side (UnityEngine, Unity.Mathematics)
- Status: no cross-module dependencies on SimTick or Vessels. Clean root of the dependency graph.

**SimTick module** (`SpaceSim.Foundation.SimTick`):
- Files: ActiveVesselCameraFollow.cs, EventPriorityQueue.cs, IActiveVessel.cs, ISimTickListener.cs, PhysicsMode.cs, SimEventType.cs, SimTickController.cs, SimTickPhase.cs, WarpController.cs, WarpHaltInfo.cs, WarpHaltReason.cs, WarpRate.cs, UI/WarpUIController.cs
- References (asmdef): SpaceSim.Foundation.Coordinates, Unity.Mathematics, Unity.Burst, Unity.TextMeshPro
- Production `using` directives: `SpaceSim.Foundation.Coordinates` (3 files: ActiveVesselCameraFollow, IActiveVessel, SimTickController). No `using SpaceSim.Foundation.Vessels;` anywhere in production sources.
- Status: clean. References Vessels concrete types only via `<see cref="...">` doc comments (which do not induce compile-time dependencies). The `IActiveVessel` interface lives in SimTick precisely so the controller doesn't depend on Vessels — the contract surface is implemented by `Vessels.Vessel` and read by `SimTick.SimTickController`.

**Vessels module** (`SpaceSim.Foundation.Vessels`):
- Files: AtmosphericEntryPredictor.cs, BodyRegistry.cs, CruiseState.cs, IVessel.cs, KeplerPropagator.cs, KeplerState.cs, OrbitalElements.cs, PeriapsisApoapsisPredictor.cs, PhysXState.cs, ReferenceBody.cs, SoiCrossingPredictor.cs, SurfaceImpactPredictor.cs, TestVesselDriver.cs, TransitionEvaluation.cs, TransitionResult.cs, Vessel.cs, VesselAuthoritativeState.cs, VesselDesign.cs, VesselEventPredictionDriver.cs, VesselRegistry.cs, VesselSoiRerootingDriver.cs, VesselTransitionDriver.cs
- References (asmdef): SpaceSim.Foundation.Coordinates, SpaceSim.Foundation.SimTick, Unity.Mathematics, Unity.InputSystem
- Production `using` directives: `SpaceSim.Foundation.Coordinates` and `SpaceSim.Foundation.SimTick` only. No outward references to other modules.
- Status: clean. Top of dependency tree.

**No cyclic patterns observed.** Dependency direction is strictly `Coordinates ← SimTick ← Vessels`. New file `ActiveVesselCameraFollow.cs` (commit 049) lives in SimTick and reads `IActiveVessel` (also in SimTick) — correct module placement.

---

## Section 9: Recurring code-level patterns

Lessons from the last 5 commit artifacts in `commits/` directory (commits 046, 047, 048, 049, 051):

**Commit 046 (SOI crossing predictor):** "Test geometry needs analytic verification before committing." Lesson is about test-authoring discipline; not a tooling gap.

**Commit 047 (atmospheric/surface predictors):** "Helper extraction post-second-caller — extract on the second or third caller, not the first." Acted on at 047 itself (extracted `OrbitalElements.SolveConicAtRadius`). The pattern continued at 053-stage1 (eccentricity helper extracted after the inline `Assert.Less(e, 0.8, ...)` pattern hit 11 call sites — well past the third-caller threshold). No tooling-improvement opportunity surfaced.

**Commit 048 (time-warp controller):** "Reconnaissance catches what specs miss." Recurring across all 5 stages of 048. This is a process lesson, not a code lesson. No tooling fix proposed.

**Commit 049 (active-vessel camera-follow):** "Spec hints are leads, not invariants" — spec's `FloatingOriginManager.OriginShift` field was actually `WorldToLocal(WorldPosition)`. Process lesson, not code.

**Commit 051 (multi-body TestVessels scene):** "Warp rates compress sim time… any 'the vessel won't get there' assertion must include 'at maximum warp rate.'" This is a specific class of test-setup bug that bit at commit 051. **Has the lesson been encoded?** Not yet — there is no linter or test-helper guarding against the warp-rate-aware sim-time check for "won't reach" assertions. Could be a future test-infra improvement, but the bot should weigh whether it's worth the abstraction cost.

**Older lessons forward-tracked:**

- "Unity.Mathematics lacks long overloads" (referenced in past audits) — checked: no new production code in the 24-hour window does math on `long` values via Unity.Mathematics in a way that would require missing overloads. `WarpController.cs` uses raw `long` arithmetic for rate calculations; correct pattern.
- "EventType collided with UnityEngine.EventType" (from `SimEventType` rename history) — no new type-name collisions surfaced in the 24-hour window.
- "Newton-Raphson convergence fails at e > 0.9" — directly addressed by commit 053-stage1's `AssertSolvableEccentricity` helper. The helper is now in place and adopted at 11 call sites across 4 test files (commit 056 migration). The lesson is structurally fixed.

---

## Section 10: Schema and architecture changes by file (24-hour window)

Foundation files modified in last 24 hours, one-line structural summary each:

- **`Coordinates/PhysicsConstants.cs`** (commit f114511 created; commit fae79ac amended) — created as new file with `EarthMassKg`, `MoonMassKg`, `MoonSoiRadiusMeters`, `EarthMu`, `DegenerateOrbitAngularMomentumSquaredScale`. Amended at 056 to add `EarthMoonDistanceMeters = 3.844e8` (migrated from `VesselTestHelpers`) and expand the doc preamble.
- **`Vessels/TransitionResult.cs`** (commit f114511 created) — new `TransitionResult` enum (`Success`, `FailedDegenerateOrbit`, `FailedOther`). Return type for `Vessel.TransitionToKeplerRails` so callers can branch on degenerate-orbit refusal vs other failures.
- **`Vessels/Vessel.cs`** (commit f114511 modified) — added degenerate-orbit guard at step 2.5 of `TransitionToKeplerRails`: computes `|h|² = math.lengthsq(r × v)` and rejects if `|h|² < scale · μ · |r|` (uses `PhysicsConstants.DegenerateOrbitAngularMomentumSquaredScale`). Method's return type changed from `void` (assumed; verify) to `TransitionResult`. Three production call sites updated to read the result per commit message.
- **`Vessels/ReferenceBody.cs`** (commit f114511 modified) — default `massKg` initializer changed from inline `5.972e24` literal to `PhysicsConstants.EarthMassKg`. Pure refactor.
- **`Vessels/TestVesselDriver.cs`** (commits 16cc1d5, f114511 modified) — added `_initialMode` field and three-toggle infrastructure for multi-vessel scenes (51 commit). Constant migrations (053-stage2 commit).
- **`Vessels/VesselTransitionDriver.cs`** (commit f114511 modified) — updated to consume `TransitionResult` return value from `Vessel.TransitionToKeplerRails`.
- **`SimTick/ActiveVesselCameraFollow.cs`** (commit 3c25ab8 created) — new `MonoBehaviour` reading `SimTickController.Instance.ActiveVessel`'s world position, converting to local, applying to camera transform in `LateUpdate`. 171 lines, single class.
- **`SimTick/WarpController.cs`** (commit fae79ac modified) — doc-comment refresh removing two stale Stage-3 forward-references at line-area 24 and line-area 340. (Line 65 stale forward-reference appears unmodified — see Section 12.)
- **`Vessels/Tests/VesselTestHelpers.cs`** (commits 4091806, f114511, fae79ac modified) — gained `AssertSolvableEccentricity(double, double = 0.8, string = null)` and `AssertNonDegenerateOrbit(double, double, double, double = 1e-10, string = null)` at 053-stage1. Lost local `EarthMoonDistanceMeters`, `MoonMassKg`, `MoonSoiRadiusMeters` declarations at 056 — now references `PhysicsConstants` directly.
- **`Vessels/Tests/VesselTestHelpersTests.cs`** (commit 4091806 created) — new test file, 85 lines, 6 helper-validation tests.
- **`Vessels/Tests/VesselTests.cs`** (commit 3b4b040 modified) — added `TransitionToKeplerRails_WhenPurelyRadialVelocity_LogsErrorAndReturnsFailedDegenerateOrbit` regression test (~74 lines).
- **9 other Vessels test files** — touched at 053-stage2 for `EarthMassKg`/`EarthMu` const migrations and at 056 for `Assert.Less(e, 0.8, …)` → `AssertSolvableEccentricity` migration. Mechanical refactors, no test-count change.
- **`Scenes/TestVessels.unity`** (commits 16cc1d5, 3c25ab8, fae79ac modified) — added Moon body + TestVessel_SoiCrossing (051); added camera-follow wiring (049); reverted scene drift at 056. Net state at 056: matches 051's canonical state.

---

## Section 11: Raw observations

- **Audit-pipeline self-reference:** the 056 commit message references "Item 3 (VesselSnapshot source-uniformity re-verification) dropped after recon found the class does not exist in the codebase. Audit reference is either stale (class renamed) or forward-looking (class planned but not built). Will surface again on next audit cycle with current commit context." This confirms a prior tech-director-audit cycle surfaced a `VesselSnapshot` reference that no longer matches reality. The tech-director-bot should note that this class doesn't exist when triaging any future mention.
- **Untracked files in working tree:** the sandbox view shows `docs/automation/tech_director_audits/` directory (with `2026-05-20.md`) plus `docs/automation/tech_director_audit_2026-05-21.md` and various `diff_*.txt` scratch files. These are not committed and represent the prior-audit-output staging area. No action needed; observation that prior runs landed here without being committed.
- **Naming inconsistency:** the trio of driver classes use the `…Driver` suffix consistently (`VesselTransitionDriver`, `VesselSoiRerootingDriver`, `VesselEventPredictionDriver`, plus `TestVesselDriver`, `TestShiftDriver`). The new `ActiveVesselCameraFollow` does not use `…Driver` — appropriately, since it's a presentation-layer reader, not a driver. Naming is internally consistent.
- **EventType reservation strategy:** `SimEventType.cs` reserves enum values for `BurnArrival` (Phase 5+) and `InterstellarArrival` (Phase 6) — pre-allocated holes in the enum surface. This is the documented extensibility approach per CONSTRAINTS §2 "Mode transitions and event scheduling." Good evidence the architecture's extensibility-hook story is real, not hypothetical.
- **TMP Essentials precedent:** commit 048-stage4 (prior to window) introduced TextMeshPro as the first UI text precedent. `SimTick.asmdef` now references `Unity.TextMeshPro`, and `WarpUIController.cs` is the example. Future UI work that needs text rendering inherits this precedent.
- **Sandbox staleness confirmed:** the file `Coordinates/PhysicsConstants.cs` reads as 144 lines in the sandbox `wc -l` but `git show HEAD:` returns 165 lines. The host repo is canonical per audit task instructions. Earlier audit-task warning honored — all findings sourced from `git show HEAD:<path>`.
- **VesselSoiRerootingDriver iteration order:** XML doc mentions "first-match-wins" iteration but the iteration uses `BodyRegistry.GetChildrenOf` which (per Section 3) returns an unsorted list. Order might be registration-order-dependent. Was previously flagged in past audit notes; no change in the 24-hour window. Carrying forward as a known item for the tech-director-bot.

---

## Section 12: Mechanical fix candidates

Three candidates surfaced this cycle. All are low-risk, single-location-or-bounded edits; doubts surfaced in the "Why it might not be mechanical" field for the tech-director-bot to weigh.

### Candidate 1: Stale `Stage 4 adds the scene wiring` forward-reference in `WarpController.cs`

- **Source section:** Section 5, Section 10.
- **Scope summary:** Update the doc-comment at `Foundation/SimTick/WarpController.cs:65` from "Stage 4 adds the scene wiring" to past-tense reflecting that Stage 4 has landed at commit `8ce6e0f` (2026-05-23). Commit 056 explicitly updated two other stale Stage-3 forward-references in the same file (lines ~24 and ~340) but missed this one.
- **Files affected:** `SPACESIM/Assets/Scripts/Foundation/SimTick/WarpController.cs` (1 file, 1 doc-comment paragraph).
- **Counts or quantities to verify:** tech-director-bot should confirm via `git show HEAD:SPACESIM/Assets/Scripts/Foundation/SimTick/WarpController.cs | sed -n '60,75p'` that the "Stage 4 adds the scene wiring" phrasing still exists.
- **Why it's mechanical:** doc-only edit. Identical refresh pattern was already executed in 056 for two other forward-references in the same file. No production code changes. No test changes.
- **Why it might not be mechanical:** the surrounding paragraph (line 60–70) is part of a "STAGE 2 SCOPE" header — the intent of that header may be to capture the historical state at Stage 2 close, in which case the forward-reference is appropriate-as-of-Stage-2 and the doc should stay. The tech-director-bot should adjudicate whether to rewrite the paragraph or leave the historical framing.

### Candidate 2: Stale `CoordinateMath.cs:25-27` "exactly one physical constant" comment

- **Source section:** Section 5.
- **Scope summary:** The comment in `Foundation/Coordinates/CoordinateMath.cs:25-27` says "Lives here in Phase 0 because there's exactly one physical constant in the codebase. When the Physics module lands…". That justification was superseded by commit 053-stage2's extraction of `PhysicsConstants.cs` into the same `Coordinates` namespace — now there are multiple physical constants co-located. The comment is no longer accurate.
- **Files affected:** `SPACESIM/Assets/Scripts/Foundation/Coordinates/CoordinateMath.cs` (1 file, 1 doc-comment paragraph).
- **Counts or quantities to verify:** tech-director-bot should confirm via `git show HEAD:SPACESIM/Assets/Scripts/Foundation/Coordinates/CoordinateMath.cs | sed -n '20,35p'` that the "exactly one physical constant" phrasing still exists, and decide whether to delete the justification or rewrite it to reference `PhysicsConstants` as the new home for additional physical constants.
- **Why it's mechanical:** doc-only edit. `PhysicsConstants.cs` already exists with its own preamble documenting the relationship to `CoordinateMath.G` ("The gravitational constant G stays in `CoordinateMath` (its existing home), not duplicated here"). The complementary update in `CoordinateMath.cs` to acknowledge `PhysicsConstants` is the same pattern.
- **Why it might not be mechanical:** the question of whether `G` should also move to `PhysicsConstants` could resurface as a design question, even though the existing `PhysicsConstants.cs` doc explicitly says it stays where it is. Tech-director-bot may want to defer if a "clean up physical-constants locality" discussion seems imminent.

### Candidate 3: Mixed-precision `MoonSoiRadiusMeters` declarations in two test files

- **Source section:** Section 5.
- **Scope summary:** `SoiCrossingPredictorTests.cs:34` and `OrbitalElementsTests.cs:319` each declare `private const double MoonSoiRadiusMeters = 6.6e7;` while the canonical `PhysicsConstants.MoonSoiRadiusMeters = 6.6183e7`. Commit 056 explicitly preserved these as "mixed-precision test infrastructure pending separate fixer-bot pass." Migrating the two declarations to `PhysicsConstants.MoonSoiRadiusMeters` would close the loop.
- **Files affected:** `SPACESIM/Assets/Scripts/Foundation/Vessels/Tests/SoiCrossingPredictorTests.cs` (1 file, 1 const declaration + 2 reference sites at lines 240, 278), `SPACESIM/Assets/Scripts/Foundation/Vessels/Tests/OrbitalElementsTests.cs` (1 file, 1 const declaration at line 319 + 2 reference sites at lines 383, 427 inside comments — production references need verification).
- **Counts or quantities to verify:** tech-director-bot should grep `git show HEAD:<file> | grep -n MoonSoiRadiusMeters` for both files to identify all consumer sites (the precision swap from 6.6e7 to 6.6183e7 is a 0.28% change that may or may not break a specific test's expected-value tolerance).
- **Why it's mechanical:** the change is a const-source swap; identical pattern was applied at commit 056 to `VesselTestHelpers.cs`. Per-call-site precision sensitivity is the only check needed.
- **Why it might not be mechanical:** commit 056's preserve-and-defer decision was explicit, suggesting the author surveyed these call sites and judged each one as "needs case-by-case sensitivity verification rather than a flat sed-style swap." The 0.28% precision shift could cause specific orbital-geometry tests to fail if they depend on the radius being exactly 6.6e7. The tech-director-bot should require a test run against the post-migration state before declaring this mechanical; alternatively, route this to a fixer-bot spec that runs the full EditMode suite after each per-site swap.

**No other candidates this cycle.** The `is_routine_supply` schema-drift observation in Section 4 is explicitly NOT a mechanical-fix candidate — it requires a design decision (is the contract right or is the implementation right?) and so belongs in tech-director-bot judgment, not a mechanical-fix queue. The `VesselSnapshot` reference dropped at 056 is also not a fix candidate — it was an audit-pipeline artifact, already resolved by the commit message's note.
