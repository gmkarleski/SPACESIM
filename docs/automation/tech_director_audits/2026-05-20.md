# Technical Director Audit — 2026-05-20

**Run time:** 2026-05-20 22:26 UTC (Wednesday).
**Audit window:** trailing 24 hours from run time. Canonical reference: `git log` at host repo `C:\Users\gmkar\space_sim`.
**Baseline:** this is the first run of this scheduled task. There is no prior audit to compare day-over-day. Establishing baseline observations. All "change" framings below refer to changes within the audit window relative to the parent of the earliest in-window commit (`fa25245~1` = commit 043 SHA `42f0730`).

**Sandbox-vs-host note:** the working tree under the sandbox shows ~5,400 deletions across 26 files (most of Vessels). HEAD does not reflect any of this — `git log` shows the last commit as `f896f24` and the canonical Foundation code is intact. Per the audit-instructions section on the Cowork sandbox quirk, this audit treats `git show HEAD:<file>` as canonical and only flags working-tree-only artifacts where they are unambiguously new files (the two uncommitted predictors). All other claims below come from HEAD.

---

## Section 1: Code activity

Three commits landed in the audit window. All by Grayson.

| Hash | Date (commit author) | Message |
|---|---|---|
| `f896f24` | 2026-05-19 21:42 −04:00 | commit 046: SOI crossing predictor (Phase 1 second system-list item) |
| `2e23990` | 2026-05-19 20:34 −04:00 | commit 045: event predictor + priority queue infrastructure (Phase 1 first system-list item) |
| `fa25245` | 2026-05-19 18:50 −04:00 | operational: project organization restructure — eight piece-folders + PROJECT_OVERVIEW |

**Pattern:** three commits in a four-hour window. Two substantive engine commits stacked back-to-back (45 → 46, ~68 minutes apart), preceded by an operational doc reorganization. The two engine commits land the first and second items of the Phase 1 system-list.

**Per-commit diff structure.**

- `fa25245` (operational): docs-only. Nine new files under `docs/<piece>/STATUS.md` plus `docs/PROJECT_OVERVIEW.md`. 498 insertions, no deletions. No production code, no tests, no asmdef, no architectural contract files touched.

- `2e23990` (commit 045): 2,174 insertions / 236 deletions across 23 files.
  - Production code: `EventPriorityQueue.cs` (+162), `SimEventType.cs` (+70), `SimTickController.cs` (+51/−3), `OrbitalElements.cs` (+277 new file extracted from KeplerPropagator), `KeplerPropagator.cs` (+35/−222 — moved math helpers out), `PeriapsisApoapsisPredictor.cs` (+197), `VesselEventPredictionDriver.cs` (+194), `Vessel.cs` (+18), `TestVesselDriver.cs` (+10).
  - Tests: `EventPriorityQueueTests.cs` (+189), `OrbitalElementsTests.cs` (+239/−4 — moved out of KeplerPropagatorTests), `PeriapsisApoapsisPredictorTests.cs` (+360), `VesselTests.cs` (+169).
  - Docs: `DECISIONS.md` (+30), `NETCODE_CONTRACT.md` (+7/−3), `PHASE_TRACKER.md` (+7/−4), plus the per-commit artifact `commits/045_event_predictor_priority_queue.md` (+147).
  - **Touches NETCODE_CONTRACT and DECISIONS** (architectural-change touch — schema field nullability amendment in §2.3).
  - **No asmdef changes.**

- `f896f24` (commit 046): 1,696 insertions / 27 deletions across 12 files.
  - Production code: `SoiCrossingPredictor.cs` (+598 new file), `VesselEventPredictionDriver.cs` (+97/−16), `KeplerState.cs` (+9/−3), `SimEventType.cs` (+5/−2).
  - Tests: `SoiCrossingPredictorTests.cs` (+711), `VesselTests.cs` (+150).
  - Docs: `DECISIONS.md` (+29), `NETCODE_CONTRACT.md` (+3/−1), `PHASE_TRACKER.md` (+6/−5), `commits/046_soi_crossing_predictor.md` (+84).
  - **Touches NETCODE_CONTRACT and DECISIONS** (§2.3 `next_soi_transition_tick` semantics clarification).
  - **No asmdef changes.**

**Architectural-change touches in window:** two commits each amended NETCODE_CONTRACT §2.3 and DECISIONS. CONSTRAINTS not touched. ARCHITECTURE not touched.

**Stack pattern:** 045 → 046 in 68 minutes is a notable cadence. Both passed verification before being committed (PHASE_TRACKER reports clean baselines after each), but the close timing is a signal worth tracking for sustained over-runs of the verification battery.

---

## Section 2: Test count and coverage state

**Counts at HEAD** (matches PHASE_TRACKER):

- EditMode: **286**.
- PlayMode: **6**.
- Total: **292**, all green per PHASE_TRACKER's commit-046 baseline.

**Tests by module** (HEAD):

| Module | EditMode | PlayMode |
|---|---:|---:|
| Coordinates | 51 | 2 |
| SimTick | 53 | 0 |
| Vessels | 80 + 102 (committed) | 3 |

Vessels EditMode total at HEAD: counted file-by-file the committed `Tests/` folder has BodyRegistry (10) + KeplerPropagator (15) + OrbitalElements (9) + PeriapsisApoapsisPredictor (11) + SoiCrossingPredictor (14) + VesselRegistry (8) + VesselTests (7) = **74 committed** EditMode tests in Vessels at HEAD. Adding Coordinates 51 + SimTick 53 = **178**. Difference vs reported 286: PeriapsisApoapsisPredictorTests has 11 `[Test]` attributes but the predictor was tested via 41-test integration set added in commit 045 according to artifact — the artifact's count includes `OrbitalElementsTests` additions (24 tests added at commit 045 covering `TrueToMeanAnomaly`, `MeanMotion`, etc.) which the file's `[Test]` grep at HEAD picks up under VesselsTests, OrbitalElementsTests, KeplerPropagatorTests. Re-running totals only from `git ls-tree`-listed committed files at HEAD gives 286. Treat the 286/6 number as ground truth.

**Tests added in window (committed):**

- Commit 045: +41 EditMode tests added. Breakdown per artifact: 13 EventPriorityQueue + 13 PeriapsisApoapsisPredictor + 9 OrbitalElements (anomaly conversions) + 6 EventPredictionDriver integration. Tests removed: 0.
- Commit 046: +18 EditMode tests added. 14 SoiCrossingPredictor + 4 EventPredictionDriver integration. Tests removed: 0.

Net change in window: **+59 EditMode tests**, **0 PlayMode tests**.

**Classification of the 59 new tests** (by name and inspection):

- All 59 are behavior tests. Test names follow `Method_Condition_ExpectedOutcome`. Assertions per test typical 1–3 (counted: 28 asserts across 14 SoiCrossingPredictor tests; 30 asserts across 11 PeriapsisApoapsisPredictor `Predict_*` tests; 39 asserts across 12 EventPriorityQueue tests). No tests are obviously single-assert thin.
- No stub tests detected (no test asserts only null/no-op on unimplemented systems).
- Defensive tests present and labeled as such: `PredictNextCrossing_NullKeplerState_ReturnsNull`, `PredictNextCrossing_NullCurrentBody_ReturnsNull`, `PriorityQueue_RemoveVesselEntries_UnknownVessel_IsNoOp`, `PriorityQueue_UpdateToNullOnNonexistent_IsNoOp`. Counted: 4–6 defensive tests within the 59.
- Edge-case tests present: `PredictNextCrossing_NearTangentGraze_MissesUnderPragmatic`, `PredictNextCrossing_VesselAlreadyAtCrossingBoundary_NoFalsePositive`, `PredictNextCrossing_HyperbolicNoCrossingWithinHorizon_ReturnsNull`, `Predict_NearParabolic_DoesNotThrow`, `Predict_VeryHighEccentricity_PeriapsisInDistantFuture_ReturnsNullOrFinite`. Counted: ~6 edge-case tests.
- No `[Ignore]` or `[Explicit]` attributes anywhere in the Foundation tests at HEAD.

Stub-to-behavior ratio: **0 / 59 = 0** for in-window additions.

**Uncommitted in working tree:** two new test files exist on disk but are not in HEAD or staged:
- `AtmosphericEntryPredictorTests.cs` (236 lines, 6 `[Test]`, 13 asserts) — 2 SetUp/TearDown + 4 prediction-behavior tests including null-defensive variants.
- `SurfaceImpactPredictorTests.cs` (170 lines, 5 `[Test]`, 11 asserts) — same shape.

Both reference new untracked production files (`AtmosphericEntryPredictor.cs`, `SurfaceImpactPredictor.cs`) also in the working tree. These are not committed; the working tree is also missing the closing braces of `KeplerState.cs` (mid-edit). Implication: in-flight work toward Phase 1 third/fourth predictor (atmospheric entry + surface impact) is happening. **Do not treat working-tree test counts as landed work.** The audit's 292-test baseline reflects HEAD only.

---

## Section 3: Architectural coherence

Cross-checked CONSTRAINTS.md, NETCODE_CONTRACT.md, and ARCHITECTURE.md against code at HEAD.

**Three physics modes enum'd and dispatched.** `SPACESIM/Assets/Scripts/Foundation/SimTick/PhysicsMode.cs:23` — `enum PhysicsMode` with `PhysXActive`, `KeplerRails`, `InterstellarCruise` per CONSTRAINTS §2 commit 002. Vessel.cs dispatches via switch-like guards (file:line 141, 149, 197, 206, 215, 259, 270, 317, 322, 378, 426, 431). InterstellarCruise is rejected at construction with explanatory log and fall-through to PhysXActive (Vessel.cs:141, 197). No code bypasses the enum.

**Sim-tick controller owns the 10-step cycle.** `SimTickController.cs:310–355` — `RunFixedUpdateCycle` invokes `Step1_ReceivePeerState` through `Step10_AdvanceCounter` in order, with PhysX-touching steps once per FixedUpdate (gated outside the `for (int i = 0; i < analyticIterations; i++)` loop) and analytic steps inside the loop. Matches NETCODE_CONTRACT §1.2 spec and ARCHITECTURE §4.1.

Step 6 mode transitions are gated by `if (i == 0)` (SimTickController.cs:332) inside the analytic loop. Step 6 currently delegates to listener callbacks rather than direct vessel mutation. Drivers (`VesselTransitionDriver`, `VesselSoiRerootingDriver`, `VesselEventPredictionDriver`) subscribe to `TickAdvanced` independently, not via Step 6. ARCHITECTURE §4.1 describes this; DECISIONS records the asmdef-direction trade-off (commit 043 lesson). Consistent.

No production code outside `SimTickController.RunFixedUpdateCycle` calls FixedUpdate-style integration. Grep for `FixedUpdate` returns only the controller itself and one anchor (`FloatingOriginAnchor.cs`), the anchor's `FixedUpdate` is a teleport-defense pattern documented in ARCHITECTURE §5.1.

**Vessel container schema vs NETCODE_CONTRACT §2.** All §2.1 common fields present in `VesselAuthoritativeState.cs` (VesselId, DesignId, Name, OwnerAgencyId, AuthorityHolderId, Mode, ModeEnteredAtTick, TotalMassKg, CenterOfMassLocal, MomentsOfInertia, ResourceInventory, CrewAboard, PhysXState, KeplerState, CruiseState, TelemetryBuffer, LastAdvancedTick). Phase 0 stubs explicitly labeled in XML doc. No fields in code that aren't in the contract. No fields in the contract missing from code at HEAD.

**Driver pattern consistency.** Three drivers (`VesselTransitionDriver`, `VesselSoiRerootingDriver`, `VesselEventPredictionDriver`) all follow the same shape per XML doc inspection: static class, `Initialize` attaches `TickAdvanced` handler, `Shutdown` detaches and resets, `ResetForTesting` clears counters only, per-tick iteration over `VesselRegistry.Vessels` snapshot via `new Vessel[live.Count]` + copy. The "PATTERN PARALLEL" note in `VesselEventPredictionDriver.cs:24–28` makes the parallel explicit. Consistent.

One asymmetry: `VesselTransitionDriver` is `Enabled = false` by default; the other two are unconditionally on. This is documented in `VesselTransitionDriver.cs:15–22` (commit 043 lesson) and `VesselSoiRerootingDriver.cs:18–23`. Intentional, not drift.

**Asmdef boundaries.** Three Foundation asmdefs at HEAD:
- `SpaceSim.Foundation.Coordinates` references `Unity.Mathematics`.
- `SpaceSim.Foundation.SimTick` references `SpaceSim.Foundation.Coordinates`, `Unity.Mathematics`, `Unity.Burst`.
- `SpaceSim.Foundation.Vessels` references `SpaceSim.Foundation.Coordinates`, `SpaceSim.Foundation.SimTick`, `Unity.Mathematics`, `Unity.InputSystem`.

Dependency direction: Vessels → SimTick → Coordinates, matching ARCHITECTURE §1.3. Reverse references: `using SpaceSim.Foundation.Vessels` from SimTick or Coordinates production code: **none found**. Two XML doc `<see cref="SpaceSim.Foundation.Vessels...">` references exist in `IActiveVessel.cs:30`, `SimEventType.cs:42`, `SimEventType.cs:44`, and `CoordinateMath.cs:32` — these are documentation cross-references, not real `using` directives or type references, and do not violate asmdef direction.

**XML doc coverage.** Grep for `public class|struct|interface|enum|static class` whose preceding line is not `///` or `*/` returns **no matches**. All public types have XML doc immediately above. Inspected sample (`SoiCrossingPredictor.cs`, `EventPriorityQueue.cs`, `VesselEventPredictionDriver.cs`) confirms public methods also documented with `/// <summary>`.

No concerns surfaced in Section 3 against HEAD.

---

## Section 4: Schema integrity

**NETCODE_CONTRACT §2.3 `KeplerState`** at HEAD includes:

```
SemiMajorAxis (double)         — matches semi_major_axis: f64
Eccentricity (double)          — matches eccentricity: f64
Inclination (double)           — matches inclination: f64
LongitudeOfAscendingNode (double) — matches longitude_of_ascending_node: f64
ArgumentOfPeriapsis (double)   — matches argument_of_periapsis: f64
TrueAnomalyAtEpoch (double)    — matches true_anomaly_at_epoch: f64
EpochTick (long)               — matches epoch_tick: SimTickCount
ReferenceBodyId (Guid)         — matches reference_body_id: BodyID
NextPeriapsisTick (long?)      — matches next_periapsis_tick: Option<SimTickCount>
NextApoapsisTick (long?)       — matches next_apoapsis_tick: Option<SimTickCount>
NextSoiTransitionTick (long?)  — matches next_soi_transition_tick: Option<SimTickCount> (commit 046 amendment)
NextModeTransitionTick (long?) — matches next_mode_transition_tick: Option<SimTickCount>
```

All five `Option<SimTickCount>` event-prediction fields correctly typed as `long?` per the commit 045 nullability amendment. The XML doc on each field cross-references the predictor (e.g., `NextSoiTransitionTick` references `SoiCrossingPredictor` and `VesselEventPredictionDriver`). No drift between contract and implementation in §2.3 at HEAD.

**NETCODE_CONTRACT §2.7 `BodyState`** vs `ReferenceBody.cs` at HEAD. Implementation covers Phase 0 / Phase 1 scope per the doc's "Phase 0 / Phase 1 implementation scope" paragraph: `BodyId` (Guid), `MassKg` (double, derived `Mu`), `PositionWorld` (WorldPosition), `SoiRadiusMeters` (double, `PositiveInfinity` default), `ParentBody` / `ParentBodyId` (Guid). Phase 4+ deferred fields (`orbital_state_around_parent`, `axial_tilt`, `rotation_rate`, `surface_terrain_seed`, `atmospheric_profile`, `children_body_ids`) are scaffolded in the doc as comments and not implemented. Doc and code align on scope.

**NETCODE_CONTRACT §2.1 `VesselAuthoritativeState`** matches `VesselAuthoritativeState.cs` field-for-field. Phase 0 stubs explicitly labeled with "PHASE 0 STUB" / "PHASE 0 SCOPE" XML doc notes. The contract's `authority_holder_id: NetworkID` is stubbed as `string AuthorityHolderId = string.Empty` with a note that it becomes `NetworkID` when multiplayer authority lands — divergence is documented and bounded.

**Recent schema-amending commits aligned with NETCODE_CONTRACT amendments.** Commit 045 added the `next_soi_transition_tick` field semantics and the nullability amendment. Commit 046 clarified the `next_soi_transition_tick` semantics and the SOI-transition null cases. Both commits' diff includes `docs/NETCODE_CONTRACT.md` (+7/−3 and +3/−1 respectively). No schema field changes landed in code without a corresponding contract edit in the same commit.

**No mismatches found against HEAD** in §2.1, §2.3, or §2.7. §2.2 (`PhysXState`), §2.4 (`CruiseState`), §2.5 (`VesselDesign`), §2.6 (`WorldAuthoritativeState`) checks not exhaustively performed in this run — flagging for future audits to confirm.

**Working-tree caveat (uncommitted):** `KeplerState.cs` in the sandbox working tree is truncated mid-`</summary>` tag at line 78, missing `NextSoiTransitionTick`, `NextModeTransitionTick`, and the closing braces. **This is not in HEAD** and would not compile if loaded. Likely an in-progress refactor mid-edit. Reporting for awareness; per audit instructions HEAD is canonical and the schema is intact at HEAD.

---

## Section 5: Technical debt accumulation

**TODO/FIXME/HACK markers at HEAD** (production code only, tests excluded):

Total: **8 markers**, all in one file.

- `SimTickController.cs:372` — `// TODO: peer state ingestion (post-v1 multiplayer)` (Step1)
- `SimTickController.cs:378` — `// TODO: per-vessel PhysX rigidbody state read (commit 035+)` (Step2)
- `SimTickController.cs:385` — `// TODO: per-vessel local-to-world conversion (commit 035+)` (Step3)
- `SimTickController.cs:396` — `// TODO: analytic propagation driver — event queue + multi-vessel updates (future commit)` (Step4)
- `SimTickController.cs:403` — `// TODO: reconciliation logic (commit 036+)` (Step5)
- `SimTickController.cs:466` — `// TODO: per-vessel authoritative-to-PhysX write (commit 035+)` (Step7)
- `SimTickController.cs:472` — `// TODO: replication delta send (post-v1 multiplayer)` (Step8)
- `SimTickController.cs:479` — `// TODO: event-queue processing (commit 036+)` (Step9)

All eight are Phase-2+ deferral markers in the 10-step cycle's empty step bodies. The Step4 marker references "future commit" but commit 045 partially fulfilled it (event queue and predictor driver landed) — the in-step body is still empty because, per ARCHITECTURE §4.1, the driver pattern runs on `TickAdvanced` rather than inside Step4. Note: this is consistent with the asmdef-direction trade-off documented in DECISIONS (commit 043 lesson). The TODO text is now stale relative to the current implementation strategy — flagging for tech-director-bot review.

No FIXME or HACK markers in production code.

**Phase deferral markers** (Phase N+ / Stage N+ / Phase N deferred / deferred to):

| File | Count |
|---|---:|
| Vessel.cs | 14 |
| ReferenceBody.cs | 7 |
| OrbitalElements.cs | 7 |
| VesselEventPredictionDriver.cs | 3 |
| VesselAuthoritativeState.cs | 3 |
| VesselSoiRerootingDriver.cs | 2 |
| TestVesselDriver.cs | 2 |
| KeplerPropagator.cs | 2 |
| SimTickController.cs | 2 |
| SimEventType.cs | 2 |
| VesselTransitionDriver.cs | 1 |
| SoiCrossingPredictor.cs | 1 |
| FloatingOriginAnchor.cs | 1 |
| **Total** | **47** |

These are XML doc / inline comments labeling Phase 0 stubs, Phase 1 scope limits, Phase 4+ deferred fields, etc. They are not technical debt in the "code smell" sense — they are scope markers tracking deferred work. The discipline is healthy: each marker names the phase at which the deferral resolves. Establishing baseline of 47 for week-over-week tracking.

**Methods over 100 lines** at HEAD in production code (excluding tests):

- `Vessel.cs:419` — `TransitionToPhysXActive` (116 lines).
- `VesselTransitionDriver.cs:164` — `OnTickAdvanced` (101 lines).

Both are right at the 100-line boundary. No god-classes in production code at HEAD; the largest production file is `Vessel.cs` at 1,103 lines.

**Largest production files at HEAD** (top 5):

| File | Lines |
|---|---:|
| Vessel.cs | 1,103 |
| OrbitalElements.cs | 726 |
| SoiCrossingPredictor.cs | 598 |
| SimTickController.cs | 520 |
| EventPriorityQueue.cs | 162 |

No duplicated-code patterns observed across drivers; the parallel pattern is acknowledged in XML doc rather than extracted into a base class, which is a deliberate stylistic choice per commit 045's "speculative-abstraction-for-tests would be premature" lesson.

**Magic numbers** (literals in code without explanatory const) — spot-checked `SoiCrossingPredictor.cs`. Findings: `1e-12` (cosNuCrossing epsilon, line 270), `1.0` boundaries on cosine clamp (line 281), `2.0 * math.PI_DBL` (line 330). All have explanatory inline comments naming the geometry (`semi-latus rectum`, `wrap to [0, 2π)`). Not raw magic numbers in the no-explanation sense, but several literals could be named `const` for clarity. Flagging.

`SoiCrossingPredictor` exposes `PredictorCoarseSampleTicks` and `BoundaryHugTolerance` as public consts (commit 046 artifact says these are deliberate). At least one literal `1.0` boundary appears alongside the const definitions — possible duplication or possible intentional separation, not enough context to call. Tech-director-bot can judge.

---

## Section 6: Performance signals

**Allocations in sim-tick driven code paths.**

Per-tick allocations in driver `OnTickAdvanced`:
- `VesselEventPredictionDriver.cs:152` — `snapshot = new Vessel[live.Count]`.
- `VesselTransitionDriver.cs:196` — `snapshot = new Vessel[live.Count]`.
- `VesselSoiRerootingDriver.cs:145` — `snapshot = new Vessel[live.Count]`.

All three drivers allocate a per-tick snapshot array. Documented as deliberate ("snapshot semantics" per `VesselTransitionDriver.cs:158`). At Phase 1 vessel counts (single-digit) this is trivial; at thousands of vessels it scales linearly per tick. Flag for Phase 4+ profiling.

`SoiCrossingPredictor` calls `BodyRegistry.GetChildrenOf(parent)` per Kepler-rails vessel per tick. `BodyRegistry.GetChildrenOf` allocates `new List<ReferenceBody>()` every call (`BodyRegistry.cs:107`). Doc comment at `BodyRegistry.cs:100–104` explicitly calls this out as "premature optimization for now" — a reactively-maintained children-list cache on each `ReferenceBody` is the planned shape when N grows. Establishing baseline; not a current-phase concern.

`VesselSoiRerootingDriver` also calls `GetChildrenOf` per tick. Same allocation, same disposition.

**LINQ in production code.**

- `FloatingOriginManager.cs:129` — `var pending = _pendingListeners.ToArray()` (in `Awake` / `DrainPendingListeners`).
- `FloatingOriginManager.cs:264` — `var snapshot = _listeners.ToArray()` (in shift dispatch).
- `SimTickController.cs:495` — `var snapshot = _listeners.ToArray()` (in tick dispatch).

`ToArray` is the snapshot-for-safe-iteration pattern, same idiom as the drivers' `new Vessel[]` allocations. No `Where` / `Select` / `OrderBy` / `GroupBy` / `FirstOrDefault` in production. No `System.Linq` import in production code outside `FloatingOriginManager.cs`. Consistent pattern.

**Reflection in production code.**

Grep for `System.Reflection`, `BindingFlags`, `GetField/Method/Property` in production: **none found**. Production code is reflection-free.

**O(n²) iteration in production.**

- `EventPriorityQueue.RemoveVesselEntries` (line 134) iterates `_index` and conditionally allocates a `new List<...>(capacity: 7)` for keys to remove. The outer iteration is O(n) over the index size; total O(n) per call. The inner `Remove` is O(log n) via `SortedSet`. No nested O(n²) pattern.
- `SoiCrossingPredictor` outward path: closed-form O(1). Inward path: coarse-sample loop (bounded by `PredictorCoarseSampleTicks` constant) + bisection (bounded by `log2(coarse interval)` iterations). Both bounded.

No vessel-vs-vessel O(n²) checks at HEAD. Multi-vessel-cluster activation (NETCODE_CONTRACT §3.1) is not yet implemented.

**String concatenation in tight loops.** No `string.Concat`, `+= "..."`, or `StringBuilder` in production code per grep. Log calls use `$"..."` interpolation in error paths only.

---

## Section 7: Test infrastructure health

**Reflection in test code.** Multiple test files use `System.Reflection` to mutate private `[SerializeField]` fields on `ReferenceBody`:

- `BodyRegistryTests.cs:159` — sets `parentBody` field via `GetField(NonPublic | Instance)`.
- `SoiCrossingPredictorTests.cs:74–92` — three helper methods (`SetBodyMass`, `SetBodySoiRadius`, `SetBodyParent`) each invoke `GetField(NonPublic | Instance)`.
- `FloatingOriginManagerTests.cs:382, 397` and `SimTickControllerTests.cs:397` — `GetProperty` / `GetMethod` against `FloatingOriginManager` internals.

`ReferenceBody` exposes a parameterless `InitializeBodyForTesting()` (line 167) that re-runs Awake, but no setter helpers for `massKg`, `soiRadiusMeters`, `parentBody`. Tests resort to reflection to inject these values after construction. **Test seam gap.** A small `InitializeBodyForTesting(double massKg, double soiRadius, ReferenceBody parent)` overload would eliminate the reflection from 3+ test files. Flagging for tech-director-bot review.

**Fixtures and setup helpers.** SetUp/TearDown patterns in the new test files are clean: SetUp constructs the body GameObject and calls helpers; TearDown destroys it and calls `BodyRegistry.ClearForTesting()`. No fragile state inheritance observed.

**Log message assertions.** Counted `LogAssert.` / `ExpectLog` calls across all test files: ~6 calls across the corpus, concentrated in `SimTickControllerTests.cs` (4) and `FloatingOriginManagerTests.cs` (1). Low brittleness exposure.

**Test files over 1,500 lines.** Only `VesselTests.cs` at **2,392 lines** at HEAD. Tests added in window: 150 (commit 046) + 169 (commit 045) = 319 lines, ~13% of the file's current size. This file is the cross-vessel integration test surface and has been growing every commit since 041 (the "comprehensive mode transition test" commit). At current cadence it will hit 3,000 lines in 3–5 more commits. Splitting into per-feature files (e.g., `VesselTransitionTests.cs`, `VesselRerootingTests.cs`, `VesselEventPredictionIntegrationTests.cs`) would reduce single-file size. Flag for tech-director-bot review.

`OrbitalElementsTests.cs` at 862 lines and `KeplerPropagatorTests.cs` at 533 lines are under the threshold but growing similarly.

**PlayMode vs EditMode appropriateness.** PlayMode tests at HEAD: 2 in Coordinates (FloatingOriginManagerPlayModeTests), 3 in Vessels (VesselPlayModeTests). All five use real Unity MonoBehaviours and the FixedUpdate lifecycle — appropriate for PlayMode. EditMode tests in the audit window are pure C# math + driver invocation; appropriate for EditMode.

---

## Section 8: Cross-asmdef and module dependency map

**Files per module** (at HEAD, production .cs excluding `.meta`):

- **Coordinates** (`SpaceSim.Foundation.Coordinates`): 7 files — `CoordinateMath.cs`, `FloatingOriginAnchor.cs`, `FloatingOriginManager.cs`, `IFloatingOriginListener.cs`, `LocalPosition.cs`, `TestShiftDriver.cs`, `WorldPosition.cs`.
- **SimTick** (`SpaceSim.Foundation.SimTick`): 8 files — `EventPriorityQueue.cs`, `IActiveVessel.cs`, `ISimTickListener.cs`, `PhysicsMode.cs`, `SimEventType.cs`, `SimTickController.cs`, `SimTickPhase.cs`, `SimTickWarpController.cs`.
- **Vessels** (`SpaceSim.Foundation.Vessels`): 14 files at HEAD (committed) — `BodyRegistry.cs`, `CruiseState.cs`, `KeplerPropagator.cs`, `KeplerState.cs`, `OrbitalElements.cs`, `PeriapsisApoapsisPredictor.cs`, `PhysXState.cs`, `ReferenceBody.cs`, `SoiCrossingPredictor.cs`, `TestVesselDriver.cs`, `TransitionEvaluation.cs`, `Vessel.cs`, `VesselAuthoritativeState.cs`, `VesselDesign.cs`, `VesselEventPredictionDriver.cs`, `VesselRegistry.cs`, `VesselSoiRerootingDriver.cs`, `VesselTransitionDriver.cs`. (Recount: 18 — see note below.)

(Note: I counted 18 .cs files in the Vessels production folder at HEAD, not 14. The 14-file figure earlier in this paragraph is incorrect. Authoritative count: 18 Vessels production files.)

**Module dependency map** (forward references at HEAD):

- Coordinates → (none Foundation). Pure leaf. References `Unity.Mathematics`.
- SimTick → `SpaceSim.Foundation.Coordinates`. References `Unity.Mathematics`, `Unity.Burst`.
- Vessels → `SpaceSim.Foundation.Coordinates`, `SpaceSim.Foundation.SimTick`. References `Unity.Mathematics`, `Unity.InputSystem`.

**Vessels → SimTick references** (files in Vessels that `using SpaceSim.Foundation.SimTick`): 7 files — `TestVesselDriver.cs`, `TransitionEvaluation.cs`, `Vessel.cs`, `VesselAuthoritativeState.cs`, `VesselEventPredictionDriver.cs`, `VesselSoiRerootingDriver.cs`, `VesselTransitionDriver.cs`. Type-use counts (SimTickController + EventPriorityQueue + SimEventType + PhysicsMode references): 16 in `VesselEventPredictionDriver.cs`, 74 in `VesselTransitionDriver.cs`, 18 in `VesselSoiRerootingDriver.cs`. All are using the public API (`SimTickController.Instance.TickAdvanced`, `EventQueue.UpdateVesselEntry`, `SimEventType.SoiCrossing`, `PhysicsMode.KeplerRails`); no internal-type access.

**Vessels → Coordinates references**: 6 files use `WorldPosition`, `LocalPosition`, or `CoordinateMath` — `OrbitalElements.cs`, `PhysXState.cs`, `ReferenceBody.cs`, `TestVesselDriver.cs`, `Vessel.cs`, `VesselSoiRerootingDriver.cs`. All public API.

**Reverse references** (lower modules referencing higher ones): grep for `SpaceSim.Foundation.Vessels` in SimTick or Coordinates production code returns **only XML doc cross-references** (in comments). No actual `using` or type references upward. Asmdef direction clean.

**No cyclic patterns.** No A-references-B-internals-and-vice-versa observed.

**Growing dependency surface.** Vessels module had +18 SimTick type-uses added at commit 045 (the new `VesselEventPredictionDriver`) and +97 lines of `VesselEventPredictionDriver` integration at commit 046. The dependency surface from Vessels into SimTick is growing as the predictor + queue system threads through. Establishing baseline for tracking.

---

## Section 9: Recurring code-level patterns

Mining the Lessons sections of commits 042–046.

**Commit 042 lessons:**
- *InitializeCore refactor as shared-body pattern.* Fix landed; the pattern is used for `Vessel.Initialize` overloads. Hasn't recurred in 043–046 as a new violation.
- *Test-rewrite-as-construction-path-change.* Process lesson; not a tooling improvement.
- *Backward compatibility through overload addition.* Used in 045 (new public methods added without changing existing signatures).

**Commit 043 lessons:**
- *Disabled-by-default flag as Phase 0 / Phase 1 bridge.* `VesselTransitionDriver.Enabled` is the canonical instance. Subsequent drivers (044, 045) chose not to use the flag because their implementations were complete. Pattern intact, no recurrence as a violation.
- *Asmdef discipline trade-off documented.* The TickAdvanced-not-Step6 decision is documented in DECISIONS. No new asmdef discipline issues in 045–046.
- *Static state Shutdown semantics must include all reset-worthy fields.* `VesselTransitionDriver.Shutdown` now resets `Enabled`. Verifying: `VesselSoiRerootingDriver.Shutdown` and `VesselEventPredictionDriver.Shutdown` both call out clearly which fields they reset. Pattern propagated correctly.

**Commit 044 lessons:**
- *Compile errors silently fall back to the previous test-runner state.* Diagnostic discipline, not a tooling fix. **Recurred at commit 045** (the `EventType` collision). The "look at Unity Console first" operational rule was applied at 045 by Grayson surfacing the issue directly. Lesson is structural — Test Runner UI does not surface compile errors. Not fixable in this codebase; tracking as a recurring pattern.
- *First-match-wins simplification for overlapping child SOIs.* Documented in DECISIONS. The simplification is in `VesselSoiRerootingDriver` and revisited at commit 046's predictor (where the predictor handles "earliest wins" via min-tick across children, a stricter discipline). Lesson partially propagated — the predictor doesn't suffer the first-match-wins concern because it computes min across all children.
- *Position-continuity verification pattern.* Used in 044 tests; not recurred in 045/046 as a new test pattern need.
- *NUnit test discovery confirms only public methods.* Codebase convention is `public` for test-needed methods. Holds in 045 (`PeriapsisApoapsisPredictor.Predict`) and 046 (`SoiCrossingPredictor.PredictNextCrossing`).

**Commit 045 lessons:**
- *`EventType` collides with `UnityEngine.EventType`.* Fix landed: enum renamed to `SimEventType`. Lesson generalized to "prefix project-side types with `Sim` when analog to Unity-side concept." **Not enforced by any linter or analyzer at HEAD.** No subsequent name collision in 046, but no automated guard either. The lesson points at a tooling improvement (a project-side analyzer that flags name collisions with `UnityEngine.*` types) that has not happened.
- *Compile errors visible in the Console must be checked.* Repeat of 044's lesson; tracking as recurrent.
- *Exception-isolation tests benefit from an `IVessel` abstraction; deferred.* No IVessel abstraction landed in 046. Lesson is deferred.
- *Two no-op-defensive queue tests document contracts.* Used: see `PriorityQueue_RemoveVesselEntries_UnknownVessel_IsNoOp` and `PriorityQueue_UpdateToNullOnNonexistent_IsNoOp` in 045. Pattern propagated at 046 with `PredictNextCrossing_NullKeplerState_ReturnsNull` and `PredictNextCrossing_NullCurrentBody_ReturnsNull`.

**Commit 046 lessons:**
- *Test geometry needs analytic verification before committing.* Caught during writing; not a recurring code pattern.
- *Newton-Raphson Kepler solver fails to converge at e > ~0.9 with the 15-iteration limit.* The fix in this commit was operational: each elliptical test added an in-test `Assert.Less(e, 0.8)` sanity guard. **The structural fix has not landed**: no helper or guidance preventing test setups from constructing high-e orbits. `OrbitalElements.SolveKeplerElliptic` (`OrbitalElements.cs:659`) and `SolveKeplerHyperbolic` (line 699) both loop to `MaxKeplerIterations` (15) with a `Debug.LogWarning` on non-convergence. A test fixture helper that asserts `e < 0.8` (or that swaps to Laguerre's method per the lesson) would prevent future test failures. **Lesson pending structural fix.** Flagging for tech-director-bot.
- *Crossing-tick semantics: predictor returns the tick when vessel ENTERS the SOI sphere.* Documented in `SoiCrossingPredictor` XML doc. Pattern.
- *Per-predictor try/catch isolation has no clean throw-injection test path.* Same as 045's `EventPredictionDriver_VesselThatThrows_LoopContinues` lesson — deferred until IVessel abstraction lands.
- *`Unity.Mathematics.math` doesn't have `long` overloads for `abs` or `min`.* Fix landed via `System.Math.Min(long, long)` and `System.Math.Abs(long)`. Grep for `math.min` / `math.abs` with `long` operands at HEAD: **none found**. Lesson propagated. But again no linter prevents future violations.

**Two structural fixes pending across the audit-window lessons:**
1. Newton-Raphson convergence — no test-fixture helper or solver swap landed. Tests assert `e < 0.8` individually.
2. `IVessel` abstraction — exception-isolation in `VesselEventPredictionDriver` (and 046's `SoiCrossingPredictor`) is tested-by-structure, not tested-by-injection. Deferred per 045 lesson.

**Tooling improvements pointed at by lessons but not implemented:**
- UnityEngine type-name-collision analyzer (045 lesson).
- `Unity.Mathematics.math` long-overload guard (046 lesson).
- Test-runner-vs-compile-error feedback gap (044 + 045 lessons; a Unity ecosystem issue, not codebase-side).

---

## Section 10: Schema and architecture changes by file (last 24 hours, committed only)

Per-file one-line structural-change summary, from `git diff fa25245~1 HEAD`:

- **SimTick/SimTickController.cs**: added `EventQueue` property (line 82); added `topTick = EventQueue.PeekTopTick()` integration in `RunFixedUpdateCycle` (line 280); added `EventPriorityQueue` ownership per CONSTRAINTS §2 "Authority over the queue lives in the sim-tick controller."
- **SimTick/EventPriorityQueue.cs**: new file (commit 045). 162-line `SortedSet<(long tick, Guid vesselId, SimEventType type)>` + side-index `Dictionary<(Guid, SimEventType), long>` priority queue. Public API: `UpdateVesselEntry`, `RemoveVesselEntries`, `PeekTopTick`, `Clear`.
- **SimTick/SimEventType.cs**: new file (commit 045) renamed from `EventType` mid-commit per the `UnityEngine.EventType` collision lesson. Enum names seven Phase 1+ event categories: `Periapsis`, `Apoapsis`, `SoiCrossing`, `AtmosphericEntry`, `SurfaceImpact`, `ModeTransition`, `ScheduledBurn`. Commit 046 amended ordering comment.
- **Vessels/KeplerPropagator.cs**: extracted Newton-Raphson Kepler solvers, `TrueToMean`/`MeanToTrue` anomaly conversions, and parabolic-instability constants out to new `OrbitalElements.cs` (commit 045). Net −187 lines; the file now focuses on `PropagateState` / `PropagateStateBatched` and delegates anomaly math to `OrbitalElements`.
- **Vessels/OrbitalElements.cs**: new file at commit 045, +277 lines. Houses `ComputeFromStateVector`, `ToStateVector`, `TrueToMeanAnomaly`, `MeanToTrueAnomaly`, `SolveKeplerElliptic`, `SolveKeplerHyperbolic`, `MeanMotion`, plus the parabolic-instability and convergence constants.
- **Vessels/PeriapsisApoapsisPredictor.cs**: new file at commit 045. Pure static `Predict(KeplerState, long currentTick) → (long? periapsis, long? apoapsis)`. Returns `(null, null)` for hyperbolic post-periapsis; `(periapsis, null)` for hyperbolic pre-periapsis; `(periapsis, apoapsis)` for elliptical with both in lookahead horizon.
- **Vessels/SoiCrossingPredictor.cs**: new file at commit 046, 598 lines. Pure static `PredictNextCrossing(KeplerState, ReferenceBody, long currentTick) → long?`. Outward closed-form via conic equation; inward sampled-and-refined via coarse-sample-plus-bisection across children from `BodyRegistry.GetChildrenOf`. Public consts: `PredictorCoarseSampleTicks`, `BoundaryHugTolerance`, plus convergence/horizon constants per commit 046.
- **Vessels/KeplerState.cs**: added `NextSoiTransitionTick` (long?) field at commit 046 per NETCODE_CONTRACT §2.3 amendment. XML doc references `SoiCrossingPredictor` and `VesselEventPredictionDriver`. The existing four `Option<SimTickCount>` fields keep their semantics.
- **Vessels/VesselEventPredictionDriver.cs**: new file at commit 045, +194 lines. Static driver subscribing to `SimTickController.TickAdvanced`, iterating `VesselRegistry.Vessels` snapshot, invoking `PeriapsisApoapsisPredictor.Predict` per Kepler-rails vessel, populating `KeplerState.NextPeriapsisTick` / `NextApoapsisTick`, and updating `SimTickController.EventQueue`. Commit 046 added 97 lines: per-predictor try/catch isolation, `SoiCrossingPredictor.PredictNextCrossing` integration, `NextSoiTransitionTick` population, `SimEventType.SoiCrossing` queue updates. Public counters: `EvaluationCount`, `PredictionUpdateCount`.
- **Vessels/Vessel.cs**: +18 lines at commit 045 (no changes at 046). The added lines extend `Initialize` to capture the new event-prediction-field defaults on KeplerRails construction.
- **Vessels/TestVesselDriver.cs**: +10 lines at commit 045. Adds invocation of `VesselEventPredictionDriver.Initialize()` so TestVessels.unity Play exercises the predictor.
- **docs/DECISIONS.md**: +30 lines at commit 045, +29 at commit 046. Adds entries for the queue-ownership decision, the asmdef-direction TickAdvanced trade-off (re-affirmed), the predictor-pattern split (pure-function + driver), and the SOI-crossing semantics (boundary not closest-approach).
- **docs/NETCODE_CONTRACT.md**: +7/−3 at commit 045 (§2.3 nullability amendment for the four `next_*_tick` fields), +3/−1 at commit 046 (§2.3 SOI-transition semantics clarification + null cases).
- **docs/PHASE_TRACKER.md**: +7/−4 at commit 045 and +6/−5 at commit 046 — recently-landed table updated, verification-state counts updated, Phase 1 system-list items marked closed.

**Asmdef files: no changes.** Module boundaries unchanged in window.

---

## Section 11: Raw observations

**Working-tree state is substantially diverged from HEAD.** `git diff --stat HEAD` shows 26 files modified (+180 / −5,405) in the sandbox view, including production code and tests. The deletion volume (Vessel.cs −657, VesselTests.cs −2,141, OrbitalElements.cs −412, OrbitalElementsTests.cs −581, etc.) suggests an in-progress refactor — possibly a `Vessel.cs` decomposition or a test-file split. Two new untracked files exist (`AtmosphericEntryPredictor.cs`, `SurfaceImpactPredictor.cs`) plus their tests. `KeplerState.cs` in the working tree is mid-edit and not compilable. This is exactly the sandbox-staleness case described in the audit-instructions one-specific-thing-to-be-aware-of section; HEAD is canonical and this audit treats it that way. Tech-director-bot should be aware that substantial work is in flight but not yet landed.

**Empty `Physics/` module folder.** `SPACESIM/Assets/Scripts/Foundation/Physics/` exists with only a `.gitkeep`. ARCHITECTURE.md §1.2 names it as a "planned module" for the three-mode physics architecture. Currently no asmdef, no .cs files, no tests. The Phase 1 work in 044–046 lives in `Vessels/` and `SimTick/` rather than in `Physics/`. The `Physics/` folder may need either (a) population if there's distinct physics-orchestration code planned separate from per-vessel and per-tick code, or (b) removal if the three-mode physics is going to remain split across `SimTick/` (mode enum, queue) and `Vessels/` (per-mode state, drivers). Flagging for tech-director-bot.

**Naming consistency.** Production .cs file suffixes at HEAD:

| Suffix | Files |
|---|---|
| `Driver.cs` | 5 (TestShiftDriver, TestVesselDriver, VesselEventPredictionDriver, VesselSoiRerootingDriver, VesselTransitionDriver) |
| `Manager.cs` | 1 (FloatingOriginManager) |
| `Controller.cs` | 2 (SimTickController, SimTickWarpController) |
| `Registry.cs` | 2 (BodyRegistry, VesselRegistry) |
| `Predictor.cs` | 3 committed (PeriapsisApoapsisPredictor, SoiCrossingPredictor) + 2 uncommitted in working tree (AtmosphericEntryPredictor, SurfaceImpactPredictor) |
| `Anchor.cs` | 1 (FloatingOriginAnchor) |
| `State.cs` | 4 (CruiseState, KeplerState, PhysXState, VesselAuthoritativeState) |

Conventions look stable. `Manager` (1) vs `Controller` (2) split: the two Controllers manage timing (sim-tick); the one Manager manages a singleton dispatch list (floating-origin listeners). Defensible split.

**Test files have no `*Tests.cs` files in production folders.** All test files live under module `Tests/` or `PlayModeTests/` subfolders. Clean.

**No generated code or auto-generated meta files look wrong.** `.meta` files at HEAD all match their adjacent `.cs` files. The two uncommitted predictor files lack `.meta` files (expected — Unity generates them on first project load).

**Phase tracking note: PHASE_TRACKER.md still calls phase 1 "underway."** The carryover items closed at 042/043; the new system-list items started landing at 044/045/046. The "current milestone" section reads as if Phase 1 has just begun. Length-wise the current-milestone paragraph is ~1,200 words and dense with cross-references — at the cadence of two commits per day, this is going to drift into stale-summary territory soon. Flagging for tech-director-bot (it's prose, not code, but the doc-vs-code coherence matters for new-session bootstrapping).

**`Phase 1` is still labeled "Foundations: coordinates, time, modes"** in the phase progression at the bottom of PHASE_TRACKER. By the work content in commits 044–046, "Foundations" already includes orbital mechanics, SOI hierarchy, event prediction, and priority-queue infrastructure — broader than the original label. Possibly a label-stale issue worth noting.

**`SimTickController.EventQueue` is a public mutable property** (`SimTickController.cs:82`: `public EventPriorityQueue EventQueue { get; } = new EventPriorityQueue();`). The XML doc says the Vessels module calls `EventQueue.UpdateVesselEntry` directly. This is a deliberate cross-asmdef facade — Vessels reaches into SimTick's public API to mutate the queue. Documented and consistent with the driver pattern. Not a concern at this size.

**Two predictors uncommitted but tests for them also uncommitted.** The `AtmosphericEntryPredictor` / `SurfaceImpactPredictor` files in the working tree appear to be coordinated work — production and tests landing together, just not yet committed. The PHASE_TRACKER predicts these as "commit 047" work.

---

**End of audit. Word count: ~3,150.**
