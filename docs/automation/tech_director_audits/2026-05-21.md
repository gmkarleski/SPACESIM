# Technical Director Audit — SPACESIM

**Date:** 2026-05-21 20:13 UTC
**Audit type:** Initial run — no previous audit to compare against. Baseline observations established here for future runs to diff against.
**Repo:** `C:\Users\gmkar\space_sim\` (Cowork-host canonical view confirmed via `git log` against host clone)

---

## Section 1: Code activity

Two commits in the last 24 hours.

**`293d2b49` — operational: ReferenceBody test seam + Physics/ placeholder resolution + SimTickController TODO sweep (2026-05-20 22:33 −0400)**

5 files changed, 128 insertions(+), 41 deletions(−). Breakdown:

- Production code (3 files): `SimTick/SimTickController.cs` (+90/−18), `Vessels/ReferenceBody.cs` (+50/−0), `Foundation/Physics/.gitkeep` (added empty placeholder; the `Foundation/Physics.meta` empty-folder marker was deleted in favor of an explicit `.gitkeep` inside the directory).
- Docs (1 file): `docs/ARCHITECTURE.md` (+11/−10).
- No tests added in this commit.
- No asmdef changes.
- No `NETCODE_CONTRACT.md` or `CONSTRAINTS.md` changes. `ARCHITECTURE.md` was edited (small wording reflow, by line count).

**`f559e4e2` — commit 047: atmospheric entry + surface impact predictors (Phase 1 third and fourth predictors) (2026-05-20 22:04 −0400)**

22 files changed, 1733 insertions(+), 205 deletions(−). Breakdown:

- New production source (2 files): `Vessels/AtmosphericEntryPredictor.cs` (+101), `Vessels/SurfaceImpactPredictor.cs` (+93).
- Modified production source (7 files): `SimTick/SimEventType.cs` (+/−12), `Vessels/KeplerState.cs` (+/−22), `Vessels/OrbitalElements.cs` (+251 — extracted `SolveConicAtRadius` and absorbed migrating helpers from `SoiCrossingPredictor`), `Vessels/ReferenceBody.cs` (+60 — added `SurfaceRadiusMeters`, `AtmosphericTopAltitudeMeters`, new test-seam overload), `Vessels/SoiCrossingPredictor.cs` (+/−202 — large net reduction; helper math migrated out to `OrbitalElements`), `Vessels/TransitionEvaluation.cs` (+/−14), `Vessels/Vessel.cs` (+/−45 — XML doc updates around `IsAtmosphericEntryPredicted`/`EvaluateTransitionTriggers`), `Vessels/VesselEventPredictionDriver.cs` (+111 — atmospheric+surface predictor invocation, exception isolation, MinNullable aggregation).
- New tests (4 files): `AtmosphericEntryPredictorTests.cs` (+242 lines, 6 [Test]), `OrbitalElementsTests.cs` (+211, +38 [Test] vs. file's previous baseline — total file is now 1073 lines with 38 [Test] attributes; the +38 is the file's pre-commit count being modified, so the actual delta is +N tests; I am unable to confirm the exact delta without reading prior version), `SurfaceImpactPredictorTests.cs` (+176, 5 [Test]), `VesselTests.cs` (+233, file is now 2625 lines).
- Architectural files touched: `NETCODE_CONTRACT.md` (+24 — added `surface_radius_meters` and `atmospheric_top_altitude_meters` to BodyState §2.7, plus mode-transition aggregation paragraph in §2.3), `DECISIONS.md` (+29), `PHASE_TRACKER.md` (+/−11), `ARCHITECTURE.md` was touched in the other commit only.
- No asmdef changes.
- `commits/047_atmospheric_surface_predictors.md` (+93 — the commit artifact).

Stacking pattern: the two commits landed roughly 30 minutes apart on the same evening, with `f559e4e2` (the substantive commit) preceding `293d2b49` (an operational cleanup). The operational commit closes out three loose ends from `f559e4e2`'s wake (test-seam method on `ReferenceBody`, placeholder Physics/ directory clarification, SimTickController TODO removal).

## Section 2: Test count and coverage state

Current test counts (`grep -c "^\s*\[Test\]"` against test files):

- Coordinates: EditMode 63, PlayMode 3 (3 [Test]/[UnityTest] combined in PlayModeTests).
- SimTick: EditMode 57, PlayMode 0 (no PlayModeTests folder for SimTick).
- Vessels: EditMode 190, PlayMode 3.
- Total: 313 EditMode tests, 6 PlayMode tests across the Foundation modules.

No `[Ignore]` or `[Explicit]` attributes found anywhere in `SPACESIM/Assets/Scripts/Foundation/`.

Tests added in commit `f559e4e2` (commit 047) — classification per file:

- `AtmosphericEntryPredictorTests.cs` (6 tests): behavior tests against the new predictor — boundary-crossing semantics, vacuum-body null return, currently-inside-atmosphere semantics, no-future-crossing null return. All assert against returned tick values or null; assertion counts look adequate per test (sampled visually).
- `SurfaceImpactPredictorTests.cs` (5 tests): behavior tests against the new predictor — surface-impact prediction, vacuum-body surface impact, orbit-above-surface null return, very-long-period overflow defense.
- `OrbitalElementsTests.cs` adds tests for the newly-extracted `SolveConicAtRadius` helper. The file has 38 [Test] attributes total; the exact delta from before commit 047 cannot be determined from git diff alone without checking out the prior version. Most look behavior-test in shape (asserting numerical results on solver outputs).
- `VesselTests.cs` adds tests at the end of file (+233 lines). File now has 83 [Test] attributes total and is 2625 lines. The +233 additions cover the mode-transition tick propagation through `EvaluateTransitionTriggers` — labeled in test names like `EvaluateTransitionTriggers_*ModeTransitionTick*`. Behavior tests.

Stub-vs-behavior ratio: all 14+ new commit-047 tests look like behavior tests (assert on predicted tick values, null returns, or transition-trigger reasons) rather than stub/defensive/edge-case-only patterns. No new tests that just assert null/no-op on unimplemented systems were added.

Single-assertion-only tests: not systematically counted; not obvious any of the new tests are single-assert. Predictor tests in particular tend to have multiple sanity-guard asserts (e.g., `Assert.Less(e, 0.8, ...)` per the commit-046 lesson).

## Section 3: Architectural coherence

LOCKED decision: **three physics modes** (CONSTRAINTS §2 "Physics architecture"). Implementation: `SpaceSim.Foundation.SimTick.PhysicsMode` enum at `SimTick/PhysicsMode.cs:21` correctly enumerates `PhysXActive`, `KeplerRails`, `InterstellarCruise`. Mode is dispatched via `VesselAuthoritativeState.Mode` and the three nullable mode-specific state pointers (`PhysXState`, `KeplerState`, `CruiseState` at `Vessels/VesselAuthoritativeState.cs:111–121`). No code observed bypassing the mode dispatch; transitions live in `Vessel.TransitionToPhysXActive` (`Vessels/Vessel.cs:419`) and the Kepler-side counterpart inside `Vessel.cs` (file is 1120 lines; symbol grep returns only one `TransitionToPhysXActive` at line 419; the reverse-direction transition is implied to exist but I did not verify by symbol).

LOCKED decision: **sim-tick controller owns the 10-step cycle** (NETCODE_CONTRACT §1.2). Implementation: `SimTick/SimTickController.cs` is 562 lines, exposes `EventQueue` and `Warp`, drives `Step10_AdvanceCounter` at line 532. No file outside the SimTick module contains `FixedUpdate` calling into sim-tick internals that I could spot from grep.

LOCKED decision: **vessel container schema matches NETCODE_CONTRACT §2**. `VesselAuthoritativeState.cs` matches §2.1 field-for-field: `VesselId`, `DesignId`, `Name`, `OwnerAgencyId`, `AuthorityHolderId`, `Mode`, `ModeEnteredAtTick`, `TotalMassKg`, `CenterOfMassLocal`, `MomentsOfInertia`, `ResourceInventory`, `CrewAboard`, `PhysXState`, `KeplerState`, `CruiseState`, `TelemetryBuffer`, `LastAdvancedTick`. Stub fields are explicitly documented as such in XML comments. No fields observed in code that are absent from the contract.

LOCKED decision: **driver pattern** (subscribe to `TickAdvanced`, iterate registry, per-vessel work). Three drivers exist:

- `Vessels/VesselTransitionDriver.cs` (line 196: `snapshot = new Vessel[live.Count]`),
- `Vessels/VesselSoiRerootingDriver.cs` (line 145: same snapshot pattern),
- `Vessels/VesselEventPredictionDriver.cs` (line 174: same snapshot pattern).

All three follow the same snapshot-then-iterate-with-try/catch shape. Consistent.

LOCKED decision: **asmdef boundaries**. Verified:

- `SpaceSim.Foundation.Coordinates` references only `Unity.Mathematics` (no back-edges into SimTick or Vessels).
- `SpaceSim.Foundation.SimTick` references `Coordinates`, `Unity.Mathematics`, `Unity.Burst`.
- `SpaceSim.Foundation.Vessels` references `Coordinates`, `SimTick`, `Unity.Mathematics`, `Unity.InputSystem`.

No cyclic references. The using-directive grep returned only `using SpaceSim.Foundation.Coordinates;` and `using SpaceSim.Foundation.SimTick;` in production files; no using of `SpaceSim.Foundation.Vessels` from inside Coordinates or SimTick code.

XML doc coverage: spot-check on public types (`PhysicsMode`, `KeplerState`, `CruiseState`, `PhysXState`, `VesselAuthoritativeState`, `ReferenceBody`) shows extensive XML documentation including cross-references to contract sections (e.g., "per `docs/NETCODE_CONTRACT.md` §2.3"). Documentation density looks high.

## Section 4: Schema integrity

Spot-checked NETCODE_CONTRACT §2.7 BodyState fields against `ReferenceBody.cs`:

| Contract field (§2.7)            | C# property                          | Type match |
|----------------------------------|--------------------------------------|------------|
| `body_id: BodyID`                | `BodyId : Guid`                      | ✓          |
| `mass_kg: f64`                   | `MassKg : double` (via `massKg`)     | ✓          |
| `mu: f64`                        | `Mu : double` (computed)             | ✓          |
| `position_world: f64x3`          | `PositionWorld : WorldPosition`      | ✓          |
| `soi_radius_meters: f64`         | `SoiRadiusMeters : double`           | ✓          |
| `parent_body_id: Option<BodyID>` | `ParentBodyId : Guid` (Guid.Empty = none) | ✓ semantically; not nullable in C# but the Empty-Guid convention is documented |
| `surface_radius_meters: f64`     | `SurfaceRadiusMeters : double`       | ✓ (added commit 047) |
| `atmospheric_top_altitude_meters: f64` | `AtmosphericTopAltitudeMeters : double` | ✓ (added commit 047) |

NETCODE_CONTRACT §2.3 KeplerState fields against `KeplerState.cs`:

| Contract                                       | C#                                     |
|------------------------------------------------|----------------------------------------|
| `semi_major_axis: f64`                         | `SemiMajorAxis : double`               |
| `eccentricity: f64`                            | `Eccentricity : double`                |
| `inclination: f64`                             | `Inclination : double`                 |
| `longitude_of_ascending_node: f64`             | `LongitudeOfAscendingNode : double`    |
| `argument_of_periapsis: f64`                   | `ArgumentOfPeriapsis : double`         |
| `true_anomaly_at_epoch: f64`                   | `TrueAnomalyAtEpoch : double`          |
| `epoch_tick: SimTickCount`                     | `EpochTick : long`                     |
| `reference_body_id: BodyID`                    | `ReferenceBodyId : Guid`               |
| `next_periapsis_tick: Option<SimTickCount>`    | `NextPeriapsisTick : long?`            |
| `next_apoapsis_tick: Option<SimTickCount>`     | `NextApoapsisTick : long?`             |
| `next_soi_transition_tick: Option<SimTickCount>` | `NextSoiTransitionTick : long?`      |
| `next_mode_transition_tick: Option<SimTickCount>` | `NextModeTransitionTick : long?`    |

All four nullable event-prediction fields are `long?` in C# matching `Option<SimTickCount>` in the contract — the commit-045 amendment aligning the contract to the implementation's honest type is preserved.

NETCODE_CONTRACT §2.1 VesselAuthoritativeState — common fields all present in `VesselAuthoritativeState.cs`. Stub fields (`OwnerAgencyId`, `AuthorityHolderId`, `ResourceInventory`, `CrewAboard`, `TelemetryBuffer`) match the contract shape with placeholder types documented in XML.

**Drift findings:** none observed. Commit 047 amended NETCODE_CONTRACT §2.7 in the same commit as adding `SurfaceRadiusMeters` and `AtmosphericTopAltitudeMeters` to `ReferenceBody.cs` — schema and implementation moved together.

**Ambiguity to flag:** `parent_body_id: Option<BodyID>` in the contract vs. `ParentBodyId : Guid` (non-nullable) with `Guid.Empty` sentinel in C#. The XML doc on `ParentBodyId` says "Guid.Empty for top-level bodies (and for self-cycle configurations rejected at Awake)." Functionally equivalent to `Option<Guid>` but uses a sentinel value rather than nullability. Consistent with the commit-045 lesson that nullables are preferred over sentinels — possible candidate for honest `Guid?` typing in a future cleanup, but flagging for tech-director-bot rather than recommending.

## Section 5: Technical debt accumulation

`TODO`, `FIXME`, `HACK`, `XXX` in production code (excluding `/Tests/` and `/PlayModeTests/`): **0 hits** for any of those markers (verified via grep against `SPACESIM/Assets/Scripts/Foundation/` with `--include="*.cs"`, then `grep -v "/Tests/"`). The SimTickController TODO sweep landed in `293d2b49`.

`Phase 2+` / `Phase 4+` / `Phase 5+` / `Phase 6` / `Stage 2+` deferral markers in production code: **66 hits** across 9 files. Sample distribution:

- `ReferenceBody.cs`: 7 mentions (Phase 4+ orbital bodies, Phase 4+ procgen, Phase 5 atmospheric flight model).
- `OrbitalElements.cs`: 4 mentions (Phase 4+ body-velocity treatment in re-rooting, Phase 4+ caching hazard).
- `CruiseState.cs`: 1 mention (Phase 6 interstellar-cruise mode).
- `SimEventType.cs`: 2 mentions (Phase 5+ maneuver nodes, Phase 6 interstellar arrival).
- `SimTickController.cs`: 2 mentions (commit 045 Stage 2/3 references).
- `AtmosphericEntryPredictor.cs`, `PeriapsisApoapsisPredictor.cs`, `SoiCrossingPredictor.cs`, `SurfaceImpactPredictor.cs`: scattered (Phase 4+ moving bodies, Phase 5/6 difficulty toggle).

All of these are XML-doc deferral markers explaining intentional scope. None are silent code-side TODOs.

Commented-out code blocks: none found. The grep for `^\s*//.*(public|private|return|new |if\s*\()` returned only XML doc-comment text (`<see cref="...">`, `<para>`) and inline explanatory comments.

Magic literals: tolerances and ceilings are explicitly named:

- `OrbitalElements.CircularThreshold = 1e-10` (line 51).
- `OrbitalElements.EquatorialThreshold = 1e-10` (line 58).
- `OrbitalElements.KeplerConvergenceTolerance = 1e-10` (line 457).
- `OrbitalElements.MaxKeplerIterations = 15` (line 464).
- `OrbitalElements.ParabolicInstabilityBand = 1e-8` (line 472).
- `OrbitalElements.BoundaryHugTolerance = 1.0` meter (line 492).

Spot-check on inline literals: `int.MaxValue` is used as a "no event in queue" sentinel in `SimTickController.cs:267, 291` and as a warp-rate ceiling in `SimTickWarpController.cs:138, 144`. Comments explain in both places. `long.MaxValue / 2` is the overflow-defense threshold in `OrbitalElements.cs:877` and `PeriapsisApoapsisPredictor.cs:182` — duplicated identical literal across two files; no const extracted. Possible future-extraction target; flagging for tech-director-bot.

Methods over 100 lines (excluding tests):

- `OrbitalElements.cs:78 ComputeFromStateVector` (140 lines).
- `Vessel.cs:419 TransitionToPhysXActive` (115 lines).
- `VesselEventPredictionDriver.cs:225 PredictAndUpdate` (130 lines).

All three are inherently sequential (state-vector → orbital elements pipeline; PhysX activation procedure; multi-predictor invocation with per-predictor try/catch). None are obvious extract-helper opportunities — they're algorithmic sequences, not coincidental concatenations.

God-class smell: not obvious from file structure. `Vessel.cs` is 1120 lines but the symbol density is high (lots of XML doc); concrete method count remains tractable.

Duplicated code patterns: snapshot pattern in three drivers (`new Vessel[live.Count]` + manual copy loop). Identical shape across `VesselTransitionDriver:196`, `VesselSoiRerootingDriver:145`, `VesselEventPredictionDriver:174`. Possible extract-helper opportunity. Also the `long.MaxValue / 2` overflow-defense in two files.

## Section 6: Performance signals

Per-tick allocations in the sim-tick hot path:

- `SimTickController.Step10_AdvanceCounter` (line 537): `var snapshot = _listeners.ToArray()`. Allocates a fresh array of `ISimTickListener` references every tick. Comment explicitly justifies it ("Iterate over a copy to tolerate listeners that unregister themselves in their callback").
- Three driver `OnTickAdvanced` paths each allocate `new Vessel[live.Count]` per tick (`VesselEventPredictionDriver:174`, `VesselSoiRerootingDriver:145`, `VesselTransitionDriver:196`) for the same reentrancy-safety reason.

That's 4 per-tick array allocations of small reference arrays for current Phase 1 vessel/listener counts. Not currently hot at Phase 1 scale (single-vessel scene). Will scale linearly with vessel count once Phase 4+ populates the home system.

LINQ in production code: 3 hits across the foundation:

- `Coordinates/FloatingOriginManager.cs:129` — `_pendingListeners.ToArray()` (inside `Register`; not per-tick — only on subscription).
- `Coordinates/FloatingOriginManager.cs:264` — `_listeners.ToArray()` inside the floating-origin shift dispatch.
- `SimTick/SimTickController.cs:537` — already noted.

No `.Where(...)`, `.Select(...)`, `.OrderBy(...)`, `.Aggregate(...)` anywhere in production code. The `ToArray()` calls are not LINQ proper (they're collection methods) but allocate similarly.

Reflection in production code: **0 hits** outside of tests. Clean.

O(n²) iteration: not surveyed exhaustively. The driver loops are O(vessels) per tick. Inter-vessel proximity checks could become O(vessels²) when multi-vessel cluster activation lands — flagging for tech-director-bot future review.

String concatenation in tight loops: `$"..."` interpolation is used for `Debug.LogError` messages in driver exception handlers (`SimTickController.cs:546, 558`; `VesselEventPredictionDriver.cs:208, 263, 291, 323, 344`; `VesselSoiRerootingDriver.cs:177`; `VesselTransitionDriver.cs:217, 261`). All inside catch blocks — only execute on exception. Not hot.

## Section 7: Test infrastructure health

**Reflection-based private-field mutation in tests:** 16 occurrences across 5 test files, all reading/setting private fields on `ReferenceBody` (`massKg`, `soiRadiusMeters`, `parentBody`, `surfaceRadiusMeters`, `atmosphericTopAltitudeMeters`):

- `AtmosphericEntryPredictorTests.cs`: 4 occurrences.
- `BodyRegistryTests.cs`: 9 occurrences.
- `SoiCrossingPredictorTests.cs`: 6 occurrences.
- `SurfaceImpactPredictorTests.cs`: 1 occurrence.
- `VesselTests.cs`: 16 occurrences (including helper methods like `SetEarthFiniteSoiAndInitialize`).

Public test seam: commit `293d2b49` added `ReferenceBody.InitializeBodyForTesting(double massKg, double soiRadiusMeters, ReferenceBody parentBody, double? surfaceRadiusMeters, double? atmosphericTopAltitudeMeters)` at `ReferenceBody.cs:256`. The new overload is used by **40 call sites** across the test suite. Comment on the overload explicitly states "Pre-existing tests using reflection-based field-mutation patterns are not migrated to this overload as part of the commit that introduced it — they migrate organically as tests get touched. The reflection pattern stays valid; this overload is an additional option, not a replacement." So the 16 reflection sites are intentionally retained until those tests get touched for other reasons. Co-existence of two seam styles in the same test files — flagging for tech-director-bot.

**Setup-helper density:** `VesselTests.cs` SetUp does reflection-set `surfaceRadiusMeters = 1.0` to neutralize the new commit-047 default (`6.371e6` Earth-like surface) for tests that don't care about surface impact. The setup also defines a `SetEarthFiniteSoiAndInitialize` reflection helper for the subset of tests that DO want a finite Earth-like body. This is a deliberate test-data convention; commented inline. Schema-evolution defense per commit 047's lesson #4.

**Log-message assertions (LogAssert.Expect):** 8+ usages across `SimTickController`, `FloatingOriginManager`, and Vessel/Kepler tests. Patterns like `LogAssert.Expect(LogType.Error, new Regex(".*listener.*threw during shift.*"))`. Brittle to wording but currently tolerable — the regexes match on stable phrasing inside the error messages.

**Large test files:**

- `VesselTests.cs`: 2625 lines, 83 [Test] attributes. Exceeds the 1500-2000 line threshold. Most likely candidate for split into per-area files (`Vessel.Mode*Tests.cs`, `Vessel.Trigger*Tests.cs`, `Vessel.MoveAndPropagate*Tests.cs`, etc.). Not flagged as urgent — but a leading indicator that future trigger/transition test additions will need a place to land.
- `OrbitalElementsTests.cs`: 1073 lines, 38 tests.
- `SoiCrossingPredictorTests.cs`: 711 lines.
- `KeplerPropagatorTests.cs`: 533 lines.

**EditMode vs PlayMode appropriateness:** PlayMode test counts are deliberately small (6 across the codebase) — `FloatingOriginManagerPlayModeTests` covers Unity-runtime-required behaviors. The rest is pure C# math living in EditMode. Looks appropriate for Phase 1.

## Section 8: Cross-asmdef and module dependency map

**Coordinates module:**

- Files: `CoordinateMath.cs`, `FloatingOriginAnchor.cs`, `FloatingOriginManager.cs`, `IFloatingOriginListener.cs`, `LocalPosition.cs`, `TestShiftDriver.cs`, `WorldPosition.cs`.
- References (asmdef): `Unity.Mathematics`.
- Cross-module dependencies in source: none (no `using SpaceSim.Foundation.*` in any Coordinates file).

**SimTick module:**

- Files: `EventPriorityQueue.cs`, `IActiveVessel.cs`, `ISimTickListener.cs`, `PhysicsMode.cs`, `SimEventType.cs`, `SimTickController.cs`, `SimTickPhase.cs`, `SimTickWarpController.cs`.
- References (asmdef): `SpaceSim.Foundation.Coordinates`, `Unity.Mathematics`, `Unity.Burst`.
- Cross-module dependencies in source: `IActiveVessel.cs` and `SimTickController.cs` both `using SpaceSim.Foundation.Coordinates;`. All Coordinates use is via public API (no internal-friend access).

**Vessels module:**

- Files: 17 production source files (predictors, drivers, state types, registries, `Vessel.cs`).
- References (asmdef): `SpaceSim.Foundation.Coordinates`, `SpaceSim.Foundation.SimTick`, `Unity.Mathematics`, `Unity.InputSystem`.
- Cross-module dependencies in source: 14 production files `using` Coordinates or SimTick, all via public types.

**Direction check:** acyclic. Vessels → SimTick → Coordinates. No reverse edges. SimTick does not reference Vessels; Coordinates references neither.

**Note on `Unity.InputSystem` reference:** the Vessels asmdef declares Unity.InputSystem as a dependency. Spot grep doesn't immediately surface where InputSystem types are used inside Vessels production code — possibly only in `TestVesselDriver.cs` for prototype scene wiring. Not a concern, but flagging that the dependency surface is broader than the pure-math files would suggest.

## Section 9: Recurring code-level patterns

Read the Lessons sections of commits 043, 044, 045, 046, 047. Engineering-relevant lessons and their status:

**Commit 043 — disabled-by-default flag as Phase 0 / Phase 1 bridge.** Status: pattern in use (`VesselTransitionDriver.Enabled` flag, defaulting off until upstream wires real values). Pattern survives commit 047 — `IsAtmosphericEntryPredicted` is now real, but the flag mechanism is preserved.

**Commit 044 — compile errors silently fall back to previous test runner state.** Engineering lesson. No tooling fix (linter/analyzer/CI check) yet to surface this automatically. Pattern recurred in commit 045 (`EventType` collision with `UnityEngine.EventType`) and was explicitly called out as a second instance in the 045 lessons. The mitigation is operational ("always check the Console first"), not structural. Tech-director-bot may want to consider whether a Unity-side analyzer or a console-pane scrape in the local dev loop is warranted.

**Commit 044 — first-match-wins iteration order in `VesselSoiRerootingDriver`.** Phase 5+ revisit item per DECISIONS. Currently implemented as documented. The Phase-1-safety argument (Hill-sphere arithmetic precludes meaningful overlap) is recorded in the lesson — flagging that an external future change to body placement could invalidate this without the driver knowing.

**Commit 045 — `EventType` collided with `UnityEngine.EventType`.** Fix landed (renamed to `SimEventType`). No tooling improvement to catch the next collision automatically. Project convention is "prefix with `Sim` when the type is a project-side analog to a Unity-side concept." Convention check: spot grep of `SPACESIM/Assets/Scripts/Foundation/` did not surface another collision-prone name being introduced post-045 (no `Event`, `Object`, `Random`, `Transform`, `Color`, `Time` as non-prefixed types in foundation modules).

**Commit 045 — exception-isolation tests deferred until `IVessel` abstraction is needed.** Lesson explicitly defers the refactor. Status: not landed; abstraction not yet created. Watch for whether a future commit independently needs `IVessel` and pulls this in.

**Commit 046 — `Unity.Mathematics.math` lacks `long` overloads.** Lesson is now a project-wide convention (fall back to `System.Math` for `long` arithmetic). Spot grep of post-046 code did not surface accidental `math.abs(long)` or `math.min(long, long)` usage in production. Commit 047 follows the convention (uses `System.Math.Min` / `System.Math.Abs` where needed). No code-side regression.

**Commit 046 — Newton-Raphson convergence fails at e > ~0.9.** Lesson recommends Laguerre's method as the Phase 5/6 swap target for high-e orbits, and the test-side mitigation is "constrain test eccentricity below 0.8 and add `Assert.Less(e, 0.8, ...)` sanity guards." Spot check on commit-047 tests: `AtmosphericEntryPredictorTests.cs` and `SurfaceImpactPredictorTests.cs` use orbits in the moderate-e range; not all new tests have an explicit `Assert.Less(e, 0.8)` guard — flagging that the lesson's preventative pattern isn't yet a universal test-writing rule, just a convention demonstrated in `SoiCrossingPredictorTests.cs`.

**Commit 047 — `BuildState` helper signature mismatch across test files.** Each test file has its own `BuildState` (with file-specific scaffolding). Commit 047's lesson noted "consolidation isn't free" and deferred. Status: four `BuildState` helpers still coexist (`OrbitalElementsTests`, `SoiCrossingPredictorTests`, `AtmosphericEntryPredictorTests`, `SurfaceImpactPredictorTests`). Future commits adding predictor tests will continue the proliferation unless the consolidation lands.

**Commit 047 — `TransitionTriggerReason.AtmosphericEntryPredicted` label imprecision.** Lesson explicitly defers the rename to a separate cleanup commit. Status: not landed. The enum value still fires for both atmospheric entry and surface impact. `Vessel.cs` IsAtmosphericEntryPredicted XML doc acknowledges this. Tracked in commit 047's "What's next" as a separable cleanup.

**Commit 047 — test SetUp default mutation as schema-evolution defense.** Pattern landed in `VesselTests.cs` SetUp (`surfaceRadiusMeters = 1.0` reflection-set). The pattern is convention-only — not enforced by a SetUp template or analyzer. Future schema additions on `ReferenceBody` will require manual application of the same pattern.

## Section 10: Schema and architecture changes by file (last 24 hours)

- `Foundation/Physics.meta`: deleted (empty-directory marker removed).
- `Foundation/Physics/.gitkeep`: added (replaces the deleted meta with an explicit empty-directory marker for git).
- `SimTick/SimEventType.cs`: cosmetic touch-up (+/−12; no new enum values added in this delta — `AtmosphericEntry` and `SurfaceImpact` already existed since commit 045 Stage 2).
- `SimTick/SimTickController.cs`: TODO sweep removal + replaced placeholder logic with `EventQueue.PeekTopTick()`-driven `ticksUntilNextEvent` computation (lines ~267–291). Earlier int.MaxValue sentinel paths now annotated with comments explaining the empty-queue case. +90/−18.
- `Vessels/AtmosphericEntryPredictor.cs`: new file. Public-static predictor `PredictNextAtmosphericEntry(KeplerState, ReferenceBody, currentTick)` returning `long?`. Solves `r(ν) = surface_radius + atmospheric_top_altitude` via the shared `OrbitalElements.SolveConicAtRadius` helper.
- `Vessels/KeplerState.cs`: XML doc updates to `NextModeTransitionTick` clarifying multi-predictor aggregation semantics. +/−22 (mostly comment delta).
- `Vessels/OrbitalElements.cs`: extracted `SolveConicAtRadius` (public static; consumed by SOI / atmospheric / surface predictors). Migrated several internal helpers from `SoiCrossingPredictor` into the public OrbitalElements API. +251.
- `Vessels/ReferenceBody.cs`: added `SurfaceRadiusMeters` and `AtmosphericTopAltitudeMeters` `[SerializeField]` properties with read-only public accessors. Defaults `6.371e6` and `0.0`. Added an overload of `InitializeBodyForTesting` accepting nullable surface/atmosphere params (commit `293d2b49` added a separate test-seam refinement on top of this). +60 (047) +50 (293d2b49) total.
- `Vessels/SoiCrossingPredictor.cs`: large net reduction (−202 of −205 in the file overall). Math helpers migrated out to `OrbitalElements.SolveConicAtRadius`. Predictor becomes a thin orchestration over the shared helper.
- `Vessels/SurfaceImpactPredictor.cs`: new file. Public-static predictor mirroring the atmospheric one, solving `r(ν) = surface_radius`.
- `Vessels/TransitionEvaluation.cs`: small XML and label-related updates (+/−14).
- `Vessels/Vessel.cs`: XML doc updates around `EvaluateTransitionTriggers` and `IsAtmosphericEntryPredicted` reflecting the multi-event aggregation. No behavioral change in the spot-checked diff; the existing trigger check now reads a richer NextModeTransitionTick value.
- `Vessels/VesselEventPredictionDriver.cs`: +111. Added atmospheric+surface predictor invocations with per-predictor try/catch isolation. Aggregates two new ticks via MinNullable into `KeplerState.NextModeTransitionTick`.
- `docs/NETCODE_CONTRACT.md`: +24. §2.7 BodyState gains `surface_radius_meters` and `atmospheric_top_altitude_meters` with full XML doc; §2.3 gains the "Mode-transition aggregation (commit 047)" amendment paragraph clarifying min-aggregation semantics and the `TransitionTriggerReason` deferred-rename.
- `docs/DECISIONS.md`: +29.
- `docs/PHASE_TRACKER.md`: +/−11 (Phase 1 system-list third and fourth entries marked complete).
- `docs/ARCHITECTURE.md`: +21/−13 (in the operational commit `293d2b49`, separately from commit 047).

## Section 11: Raw observations

- **Driver-naming consistency:** the three Phase 1 drivers all use the `…Driver` suffix (`VesselTransitionDriver`, `VesselSoiRerootingDriver`, `VesselEventPredictionDriver`). No `…Service` or `…Manager` in the Vessels module that compete with the suffix convention. `FloatingOriginManager` and `BodyRegistry` use different suffixes — `Manager` is reserved for the floating-origin singleton (sensible naming), `Registry` for the body and vessel ID-keyed lookup tables.
- **`PrototypeStartupTest.cs` lives directly inside `Foundation/`,** not inside a module folder. Production code outside the three module folders. Verified: it sits at `SPACESIM/Assets/Scripts/Foundation/PrototypeStartupTest.cs`. Possibly intentional prototype scaffolding; flagging because it doesn't belong to any module's asmdef.
- **Empty `Foundation/Physics/` directory** marked by `.gitkeep` (added in `293d2b49`). The directory is reserved for future Phase 3+ physics-tier code. Flagging because empty directories with placeholder files are easy to forget about — recommend the tech-director-bot watches whether content lands here vs. it being deleted later.
- **`Vessels/PlayModeTests/` directory exists** but contains very few [Test]/[UnityTest] attributes (the survey returned 3). Possible under-investment in Vessels PlayMode coverage — but most Vessels work is pure orbital math, so EditMode is the right home for the bulk. Flagging without judgment.
- **`Vessels` asmdef references `Unity.InputSystem`** while the only production file using it appears to be `TestVesselDriver.cs` (prototype-input wiring). The dependency surface is wider than the math-and-state code strictly needs.
- **`OrbitalElements.cs` is becoming a public-helpers hub.** `SolveConicAtRadius` extracted at commit 047 is its second extraction (the first was `ReRootStateVector` at commit 044). Both extractions migrated math out of a specific predictor into the shared module. Healthy refactoring pattern — the "second or third caller" rule from commit 047's lesson is being applied. Worth watching whether the file grows past a tractable size — it's already a ~1000-line file based on line-number references (line 877 for the overflow check, line 957 for hyperbolic solver). I did not measure exact size; the line numbers seen in grep suggest roughly 1000+ lines.
- **Two-style test seam coexistence:** the recently-added `InitializeBodyForTesting(double, double, ReferenceBody, double?, double?)` overload at `ReferenceBody.cs:256` and the older reflection-on-private-field pattern coexist within the same test files. Tests have explicit comments documenting which seam to use and why (point-mass default + per-test reflection override). Not a defect, but a notable bifurcation in the test-data convention.
- **No save/load implementation has begun.** `Commit 047`'s "What's next" calls out "Save/load format as parallel track. Independent of the predictor / warp work; can land in any Phase 1+ commit." Phase 1 has been adding predictors aggressively (commits 043–047, five commits in eight days). Flagging that schema field additions are accumulating ahead of save/load — when save/load lands, it will need to handle every field added across commits 043–047 at once.
- **No code-style drift observed.** Indentation is consistent (4-space). Brace style is Allman (newline before opening brace). XML doc comments are uniformly present on public types.
- **Auto-generated `.meta` files** are tracked in git and look consistent with the directory structure (no orphan `.meta` files seen in the recent commit listings except the deliberate `Physics.meta` → `Physics/.gitkeep` move).
- **No new `using` directives appeared in production code in the last 24 hours that weren't already conventional.** Spot grep returned only Coordinates and SimTick using-statements in foundation source.
